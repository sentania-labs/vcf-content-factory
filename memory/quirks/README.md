# Quirks

API weirdness, tool gotchas, one-off oddities that don't rise to rule level.

Each quirk file should document:
- What the quirk is
- When it was discovered
- How to work around it
- Why it's not promoted to a rule (usually: too narrow, vendor-specific, or non-reproducible)
