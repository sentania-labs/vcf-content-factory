# Environment

User-specific lab environment details. Not committed to the main repo branch
(add to .gitignore if this contains sensitive hostnames or credentials).

Create files here for:
- Instance URLs and profiles (devel, prod, qa)
- Lab naming conventions
- User-specific preferences

Example: `lab-profile.md` with instance URLs, admin users, and environment names.
