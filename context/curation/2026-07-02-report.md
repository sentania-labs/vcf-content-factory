# Curation report — 2026-07-02

Corpus scanned: rules/ (14, +INDEX.md) · lessons/ (22, +INDEX.md) ·
context/ (249 files across top-level + subdirs) · .claude/agents/ (20) ·
skills (5)
Since: 2026-06-29 (prior report)
Findings: 6  (2 mechanical / 4 judgment)

## Mechanical (orchestrator may apply as a small PR)

- [INDEX ROT] `context/README.md` — `context/feedback_queue.md` and
  `context/vrops-content-management.md` exist on disk at the context/
  top level but have no row in `context/README.md` (neither the
  "Always read" table nor any other section references them; both
  files are legitimate, non-trivial docs — `feedback_queue.md` is the
  intake queue for user feedback, `vrops-content-management.md` is an
  extracted vROps/Suite-API reference doc). → Add rows (probably
  "Reference files" or a new small section for `feedback_queue.md`
  given its "intake, triage routes it" semantics).
- [INDEX ROT] `context/README.md` — the `cleanroom-spec/` subdirectory
  is documented (`### cleanroom-spec/ — Cleanroom findings`) but the
  sibling `context/cleanroom-requests/` directory (4 files:
  `2026-06-30-CORRECTION-cp-stitches-via-casa.md`,
  `2026-06-30-cross-mp-foreign-relationship-mechanism.md`,
  `2026-06-30-oracle-mssql-foreign-vm-identity.md`,
  `2026-06-30-stitch-survey-avi-srm-openmanage-mongo-nsx.md`) has no
  entry anywhere in the README. → Add a one-line "read on demand"
  section entry (these are the outbound cleanroom-decompile request
  briefs, distinct from `cleanroom-spec/`'s findings).

## Judgment (triage — Scott)

- [STALE-FACT] `HOW_IT_WORKS.md:265` — "Tier 2 Phase 1 is complete
  (framework JAR + builder + hello-world adapter); Synology and Dell
  PowerEdge are queued as the first real Tier 2 adapters." Both
  shipped long ago (synology is at build 26+ with live prod stitch
  proof; Dell was tried and superseded by
  `lessons/dell-redfish-relationship-strategies.md`'s "requires Tier 2"
  finding but the roster now also includes unifi, compliance,
  vcommunity* as real Tier 2 adapters). This is, verbatim, the exact
  motivating STALE-FACT example quoted in the curator's own design doc
  (`designs/curator-v1.md:60-61`) and agent prompt
  (`.claude/agents/curator.md:81`) — it was never actually fixed in
  `HOW_IT_WORKS.md` itself. → Reword to reflect current Tier 2 roster
  (synology, unifi, compliance, vcommunity-*) and drop the "queued"
  framing.
- [SUPERSEDED] `context/session-handoff.md` (dated 2026-06-14,
  self-labeled "Transient working-state note... delete once
  consumed") — headline claims "No open PRs. vCommunity is still
  parked on a credential/log blocker only Scott can clear," and its
  "durable state" section describes the defect registry as of that
  date (DEF-001–004 only; DEF-005/006 didn't exist yet). Superseded by
  ~3 weeks and many PRs of history: DEF-005 and DEF-006 opened,
  investigated, and closed today (`context/defects.md`); the
  cross-MP-stitch identity work (build 26, ambient credential v3) and
  the new lesson `lessons/cross-mp-stitch-cp-identity-and-edge-mechanics.md`
  landed; `designs/managementpacks/vcommunity-parity-plan.md` shows
  vCommunity is no longer simply "parked." Prior curation report
  (2026-06-29) already flagged a slice of this as SUPERSEDED-19 (DEF-001
  status) and marked it low-priority "delete once consumed" — it has
  now sat stale for a full curation cycle plus this one. → Given the
  file explicitly says delete-when-consumed and is >2 weeks past its
  own relevance window, recommend deleting it outright rather than
  updating it again.
- [SUPERSEDED] `designs/sdk-adapters/synology-declarative-stitch-experiment.md:16-17`
  and `designs/stitcher-identity-and-additive-foreign-v1.md:69` — both
  assert as settled fact that "Oracle's stitch is
  `relationships|VirtualMachine_parent = oracledemo`, zero adapter API
  calls" / "Oracle puzzle solved (describe.xml traversal, no suite-api
  calls)." This premise was disproven the same day it was investigated:
  `context/investigations/recon_log.md`'s 2026-07-01 Oracle entry,
  `context/investigations/cp-auth-door-probe-2026-07-01.md` Q3, and
  `context/api-maps/tvs-datastore-binding-value.md` all got a dated
  **CORRECTION (2026-07-02)** appended reversing the claim (real
  mechanism: a per-cycle Suite API read under the platform-injected
  per-instance credential — see
  `context/investigations/oracle-stitch-autopsy-2026-07-02.md` and
  `context/investigations/platform-edge-engine-2026-07-02.md`). These
  two design docs cite the disproven claim as their evidentiary premise
  and have **no** correction note, unlike the three investigation docs
  that did get one. Low operational risk (the experiment they motivated
  — build 25 — is superseded by build 26's working mechanism per
  `context/defects.md` DEF-006 closing evidence), but a future reader
  of either design file alone would re-absorb the false "zero API
  calls" story. → Append a short correction pointer to both (same
  pattern as the three corrected docs), or note at the top that the
  premise was superseded by DEF-006's closing evidence.
- [CONTRADICTION] `lessons/setrelationships-foreign-adapter-scoped.md`
  vs `lessons/cross-mp-stitch-cp-identity-and-edge-mechanics.md` (and
  the design that drove the change,
  `designs/stitcher-identity-and-additive-foreign-v1.md`). The older
  lesson (2026-06-10, synology build 16) states as its **Rule**: "The
  full-set `parentForeign` + `build()` form is safe... **Do not add
  delta/labeled emission workarounds** (`addRelationships`,
  `setGenericRelationships(…, label)`) to protect foreign parents'
  existing edges." The newer lesson (2026-07-02, synology build 26)
  states as fact #2: "the working pattern is:... push the edge
  **additively**... never full-set onto a foreign parent." The design
  doc that authorized the change is explicit that this is a deliberate
  reversal — a "write-verb split" retiring the old full-set-on-foreign
  idiom for foreign parents specifically because the 9.0.2 proof behind
  the old lesson was never re-verified on 9.1 (the old lesson's own
  "Open residual" section flags this). Per CLAUDE.md precedence, rules
  beat lessons, but between two lessons on the same situation the
  newer, live-proven-on-9.1-relevant-path guidance should govern, and
  right now a reader hitting the older lesson first (it's cited earlier
  in `lessons/INDEX.md` and by `context/tier2_architecture.md`) would
  do the opposite of what the 2026-07-02 lesson and the shipped
  framework code (`RelationshipBuilder`'s additive-on-foreign path)
  actually do. → Add an addendum to
  `setrelationships-foreign-adapter-scoped.md` (same pattern as the
  addendum already used in
  `lessons/suite-api-stitch-ssl-tofu-vs-java-http.md`) pointing to the
  write-verb split and noting the "do not add delta workarounds"
  guidance no longer applies to foreign-parent edges.

## Coverage notes

- **Fully scanned:** `rules/` vs `rules/INDEX.md` (clean, no drift);
  `lessons/` vs `lessons/INDEX.md` (clean — the prior report's
  INDEX-ROT-16 finding is fixed); `context/README.md`'s top-level file
  table and named subdirectory sections vs `context/*.md` and
  `context/*/` on disk; `context/defects.md` DEF-005/006 lifecycle
  cross-checked against every doc citing those IDs; the three
  2026-07-01 investigation docs named in the brief and their
  CORRECTION addenda; the new lesson vs the four named sibling stitch
  lessons; `RULE-014` vs `cli.py`/`sdk_builder.py`/`buildkit.py` version
  stamping code (consistent, no drift); `content-installer.md` and
  `sdk-adapter-author.md` frontmatter vs the CLAUDE.md roster table
  (both consistent — `content-installer`'s pak-install charter matches
  the "runs CLI" writes-to cell and CLAUDE.md's own delegation-protocol
  text; `sdk-adapter-author`'s `model: sonnet` has no stray `opus`
  citation elsewhere in the corpus); RULE-006 citations post-471365e
  renumbering (all current, no stale RULE-006-011 leftovers found
  outside the renumbering's own commentary).
- **Sampled, not exhaustive:** the full `.claude/agents/*.md`
  writes-to/spawn-when diff against the CLAUDE.md roster (checked
  `content-installer` and `sdk-adapter-author` specifically per the
  brief's flagged churn; did not re-diff all 20 prompts this pass —
  the 2026-06-13 and 2026-06-22 reports covered this class more
  broadly and no new roster edits landed besides those two). DUPLICATION
  was checked only for the stitch/SSL cluster named in the brief
  (clean — the new lesson complements rather than duplicates its
  siblings once the CONTRADICTION above is resolved); did not re-survey
  the wider corpus for new duplication clusters this pass.
- **memory/environment/** — `user-preferences.md` and
  `TODO-top-level-reorg.md` are new but not orphaned: both are
  self-documenting (dated, sourced, with explicit status/origin
  fields) and `TODO-top-level-reorg.md` explicitly declares itself
  disposable ("delete when implemented"). `memory/INDEX.md` already
  describes `environment/` as holding exactly this kind of file
  generically. No finding; noted for visibility only.
- **No findings this pass:** DEAD-REF (rule-ID/lesson-name/skill-name
  citations spot-checked, all resolve); DRIFT (path references spot-
  checked in agent prompts touched this session, all resolve);
  PROMPT-ROSTER SKEW (the two flagged prompts are consistent with
  CLAUDE.md).

## Summary for orchestrator

- Counts by class: INDEX ROT 2 (mechanical) · STALE-FACT 1 ·
  SUPERSEDED 2 · CONTRADICTION 1 (judgment).
- Mechanical/judgment split: 2 / 4.
- Highest-value finding: the **CONTRADICTION** between
  `lessons/setrelationships-foreign-adapter-scoped.md` and the new
  `lessons/cross-mp-stitch-cp-identity-and-edge-mechanics.md` — a
  future Tier 2 author (unifi is named next) who reads only the older,
  earlier-indexed lesson will implement the now-wrong full-set-on-
  foreign-parent idiom that the framework itself no longer uses.
  Second-highest: the STALE-FACT in `HOW_IT_WORKS.md` is notable
  because it is the literal, still-unfixed example that motivated this
  agent's own design doc.
- Report path: `context/curation/2026-07-02-report.md`.
