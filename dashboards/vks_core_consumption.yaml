id: 62652554-b2a7-46ef-a7cd-8163ab3b59f1
name: "[VCF Content Factory] VKS Core Consumption"
description: >
  Per-vCenter breakdown of VM vCPU by role (VKS Worker, Supervisor CP, vCLS,
  VM Service, Regular) plus physical cores, vCPU:pCore ratio, and the computed
  VKS core equivalent. One row per vCenter — no scoping widget, this is a
  read-at-a-glance report. Replaces the per-vCenter PowerCLI VKS core-
  consumption script.

widgets:
  - id: vks_core_view
    type: View
    title: "VKS Core Consumption by vCenter"
    coords: {x: 1, y: 1, w: 12, h: 11}
    view: "[VCF Content Factory] VKS Core Consumption by vCenter"
    self_provider: true
    pin:
      adapter_kind: VMWARE
      resource_kind: vSphere World
