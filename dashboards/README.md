# dashboards/ — legacy reference files

This directory is NOT the content source for VCF Ops dashboards.

Content authors: use `content/dashboards/` for all dashboard YAML files that
the factory authors, validates, and packages.

This directory contains legacy reference files retained for historical
context. Do not add new content here.
