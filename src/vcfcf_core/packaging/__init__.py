"""Packaging with no factory-tree dependency: constants and routing (row 1:
release_types, template_version); bundle manifest parsing, dependency
extraction, the describe-cache reader and merge, the audit, and in-memory
distribution zip assembly (row 3: loader, deps, describe, audit, assembly)."""
