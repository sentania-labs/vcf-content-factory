"""Report helpers. Empty in row 1: ``vcfcf_reports.render`` imports the row-3
loader (``ReportDef``, ``Section``, ``_STATIC_CONTENT_KEYS``) at runtime, so
it lands together with that loader rather than ahead of it."""
