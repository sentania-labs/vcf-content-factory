# Curation report — 2026-06-22

Corpus scanned: rules/ (13) · lessons/ (21 on disk, 19 indexed) · context/ (~135 files
                across root + mpb/ + authoring/ + api-surface/ + wire-formats/ + api-maps/ +
                reviews/ + investigations/ + other subdirs) · .claude/agents/ (20) ·
                .claude/skills/ (5) · .claude/commands/ (4)
Since: 2026-06-13 (last curation report)
Findings: 6  (1 mechanical / 5 judgment)

---

## Prior report status

All four mechanical findings from 2026-06-13 were resolved before this audit:

- [FIXED] knowledge/lessons/INDEX.md now has the `pak-uninstall-cascades-credentials.md` row (e8ba2e0).
- [FIXED] knowledge/context/README.md now has entries for `framework_v2_migration.md`,
  `view_dashboard_design_guide.md`, `mpb_import_investigation.md`, `reference-mpb-research.md` (bd54f9e).
- [FIXED] mp-author.md now uses `content/managementpacks/` throughout (bd54f9e).
- [FIXED] CLAUDE.md roster now shows `bundles/, dist/` for content-packager (bd54f9e).

The three judgment findings from 2026-06-13 regarding `tier2_architecture.md` volatile status
were also resolved: PR #23 (e8ba2e0) removed the stale phase/build-number block and codified
`knowledge/lessons/no-volatile-status-in-reference-docs.md` as prevention.

---

## Mechanical (orchestrator may apply as a small PR)

### [INDEX ROT] knowledge/lessons/INDEX.md — two new lessons on disk with no index entries

Two lesson files were written during the vCommunity sprint (per `context/session-handoff.md`,
they were "intentionally left uncommitted / lesson INDEX rows dropped from #21 to avoid dead
links — Codex catch"). Both files are now on disk as untracked but have no INDEX row.

Files on disk:
- `knowledge/lessons/cross-runtime-pak-upgrade-split-brain.md`
- `knowledge/lessons/stitch-moid-not-unique-across-vcenters.md`

Evidence: `ls lessons/*.md` shows both files; `grep "cross-runtime\|stitch-moid" knowledge/lessons/INDEX.md`
returns nothing.

Proposed fix: Add two rows to `knowledge/lessons/INDEX.md`:

```
| [cross-runtime-pak-upgrade-split-brain.md](cross-runtime-pak-upgrade-split-brain.md) | On VCF Ops 9.0.2, installing a same-identity classic Java SDK pak over a containerized (Python iSDK) pak silently upgrades the manifest but never replaces the DOCKERIZED adapter registration. Use a distinct adapter kind key; no in-place migration path exists. |
| [stitch-moid-not-unique-across-vcenters.md](stitch-moid-not-unique-across-vcenters.md) | Foreign-resource stitchers that match on bare MOID silently corrupt multi-vCenter deployments. Scope resolution to the owning vCenter UUID via VMEntityVCID. Fix pattern: vcommunity build 2 + compliance build 51. |
```

Note: the lesson text for `stitch-moid-not-unique-across-vcenters.md` will also need the SUPERSEDED
update described below before this is committed.

---

## Judgment (triage — Scott)

### [SUPERSEDED] `knowledge/lessons/stitch-moid-not-unique-across-vcenters.md:5` and `:73–74` — "fix queued" language still present

The lesson (line 5): `**Same defect in the compliance adapter's stitcher — fix queued.**`

The lesson (lines 73–74):
```
`content/sdk-adapters/compliance/` — ComplianceStitcher carries the same defect;
fix queued. Until then it is NOT the pattern to copy for stitching identity.
```

The fix landed in compliance build 51. Evidence:

- `content/sdk-adapters/compliance/src/com/vcfcf/adapters/compliance/ComplianceStitcher.java`
  lines 28–51: Javadoc reads "Cross-vCenter MOID scoping (the MOID trap — fix, build 51)."
  `setOwningVcUuid(String vcUuid)` is implemented (line 99); `VMEntityVCID` scoping logic
  is present (lines 161, 177, 215–218).
- `context/session-handoff.md` line 57: "DEF-004 candidate: compliance MOID fix built (build 51,
  pushed c126194) but devel still on build 50 — register per RULE-012 if unverified at next
  compliance review."
- `context/session-handoff.md` line 226: "compliance build 51 carry the fix."

The "fix queued" and "NOT the pattern to copy" warnings are now obsolete. The fix is built and
pushed to the pak's own remote; the only open question is devel installation verification
(a DEF-004 graduation candidate per RULE-012, not a "fix queued" state).

Proposed fix: Update lines 5 and 73–74 to reflect that the fix landed in compliance build 51
(commit c126194). The lesson's historical context (the vcommunity review catching the bug,
the fix pattern) remains valuable — retire only the "fix queued / NOT the pattern" warning.

---

### [CONTRADICTION] `knowledge/lessons/stitch-moid-not-unique-across-vcenters.md:73–74` vs `.claude/skills/vcfops-sdk-adapter/SKILL.md` frontmatter `:13`

The lesson (line 73–74): `ComplianceStitcher carries the same defect; fix queued. Until then
it is NOT the pattern to copy for stitching identity.`

The skill (frontmatter, line 13): `The compliance adapter (content/sdk-adapters/compliance/) is
the reference implementation.`

These assert incompatible guidance: the skill says compliance is the reference; the lesson says
compliance is NOT the pattern to copy for stitching. As of compliance build 51, the skill is
correct. The lesson is the stale party (same SUPERSEDED finding above).

Precedence per CLAUDE.md: lessons win over context, and the skill is more analogous to context.
But in this case the lesson is superseded by a concrete fix — the skill is the accurate statement.

Proposed fix: Resolve via the SUPERSEDED update to the lesson. Once the lesson's "NOT the pattern"
language is retired, this contradiction disappears. No change needed to the skill.

---

### [DRIFT] `alert-author.md`, `supermetric-author.md`, `symptom-author.md`, `report-author.md` — bare write paths (not `content/`)

Four agent prompts use bare directory paths for their write targets and validate commands.
There are no bare `alerts/`, `supermetrics/`, `symptoms/`, or `reports/` directories at the
repo root — all content lives under `content/`.

Evidence per agent:

| Agent | Stale path in prompt | Actual path | Key lines |
|---|---|---|---|
| `alert-author.md` | `alerts/`, `recommendations/` | `content/alerts/`, `content/recommendations/` | lines 9, 19, 36, 55, 97, 105, 148, 151, 153 |
| `supermetric-author.md` | `supermetrics/` | `content/supermetrics/` | lines 9, 29, 68, 90, 104 |
| `symptom-author.md` | `symptoms/` | `content/symptoms/` | lines 9, 19, 49, 50, 77, 84 |
| `report-author.md` | `reports/` | `content/reports/` | lines 9, 18, 50, 75, 82 |

Disk confirmation: `ls content/` shows `alerts/`, `supermetrics/`, `symptoms/`, `reports/`
all present; `ls alerts/` (bare) → directory does not exist.

CLAUDE.md roster (lines 65, 69, 70, 71) already shows the correct `content/` paths for all
four agents. This is the same class of drift that afflicted `mp-author.md` (fixed in the prior
curation cycle via bd54f9e) and is documented as the sentinel failure in
`knowledge/lessons/content-root-is-content-dir.md`.

Also: `supermetric-author.md:67` validate command reads
`python -m vcfops_supermetrics validate supermetrics/<file>.yaml` — the bare path
argument would fail to resolve at runtime.
`symptom-author.md:49` reads `python -m vcfops_symptoms validate symptoms/<file>.yaml` — same problem.

Proposed fix: Replace all bare `alerts/`, `recommendations/`, `supermetrics/`, `symptoms/`,
`reports/` with their `content/` prefixed equivalents in all four agent prompts. Update validate
command path arguments where they include the bare path. Mechanical find-and-replace, same
pattern as the mp-author fix.

---

### [DUPLICATION] `knowledge/lessons/stitch-moid-not-unique-across-vcenters.md` and the MOID trap section of `.claude/skills/vcfops-sdk-adapter/SKILL.md` — overlapping guidance, risk of divergence

The lesson (`stitch-moid-not-unique-across-vcenters.md`) and the SDK adapter skill
(`vcfops-sdk-adapter/SKILL.md` §"ARIA_OPS stitching identity — the MOID trap") both
state the same rule: MOID is not unique across vCenters; scope by vCenter UUID + MOID.

The skill (SKILL.md:117–130) states: "Join on a stable key: FQDN hostname, or vCenter
`instanceUuid` + MOID. Getting this wrong is silent data corruption."

The lesson (lines 40–57) states the identical rule and adds the vcommunity build 2 fix pattern.

These are complementary, not conflicting, but they cover the same ground and currently
disagree on one point (the SUPERSEDED "NOT the pattern" claim). Risk: if the lesson is
updated but the skill is not (or vice versa), future sessions get contradictory guidance
on whether compliance is safe to copy for stitching.

Proposed fix (judgment call): After the SUPERSEDED update to the lesson, add a `## See also`
cross-link in the lesson pointing to `vcfops-sdk-adapter/SKILL.md §ARIA_OPS stitching identity`.
No content change to the skill needed. This makes the relationship explicit so a future update
to either stays visible.

---

### [STALE-FACT] `context/session-handoff.md` header — date and sprint description no longer current

`context/session-handoff.md` line 1:
```
# Session Handoff — 2026-06-14 (orchestration-review sprint + 9.1 bundle + curation/hygiene — ALL MERGED; vCommunity parked)
```

Today is 2026-06-22. Multiple sessions (PR #24, Codex fix) have landed since June 14. The
"vCommunity parked" status may also have changed (vcommunity build reviews through build-6
are now on disk, and new investigations exist).

Evidence: `context/session-handoff.md:1` timestamp + git log shows commits 5fd5bac and ad9b525
(the SDK content emit pipeline, merged 2026-06-17/18) landed after the June 14 handoff. New
untracked files include `knowledge/context/reviews/vcommunity-build-3.md`, `-build-4.md`, `-build-6.md`,
`knowledge/context/investigations/vcommunity-solutionconfig-400-2026-06-16.md`, and
`knowledge/designs/managementpacks/vcommunity-parity-plan.md` — suggesting the vCommunity work is
no longer fully "parked."

Note: `session-handoff.md` is explicitly described as "not durable knowledge — delete once
consumed." This finding is lower priority than the structural corpus rot above; it is flagged
only because the handoff is the primary orientation document for a fresh session, and a stale
header creates incorrect expectations about what is in-flight.

Proposed fix: Update `context/session-handoff.md` to reflect the current state (PR #24 landed,
vCommunity progress since June 14, new build reviews). Scott's call — this may be part of the
normal session-start workflow.

---

## Coverage notes

**Fully scanned:**
- `knowledge/rules/INDEX.md` vs disk — all 13 rules present; no index/disk mismatch; rule files exist.
- `knowledge/lessons/INDEX.md` vs disk — 2 missing entries found (cross-runtime + stitch-moid).
- All `.claude/agents/*.md` frontmatter + writes-to vs CLAUDE.md roster — 4 DRIFT findings
  (alert-author, supermetric-author, symptom-author, report-author); all other agents match.
- `knowledge/context/README.md` vs selected disk directories — no new missing entries beyond the new
  investigations/ files (which are intentionally un-indexed per README architecture: "read when
  debugging a specific issue; not required reading").
- All prior-report mechanical findings — confirmed fixed.
- All RULE-ID citations (grep across corpus) — no dangling IDs; RULE-012/013 consistently cited.
- Skill name citations in agent prompts vs `.claude/skills/` on disk — all 5 match.
- `knowledge/designs/curator-v1.md` and all cited design files — present.
- `knowledge/context/defects.md` — DEF-001 (synology, open), DEF-002 (unifi, open), DEF-003 (synology,
  closed). No DEF-004 exists yet (session-handoff labels it a "graduation candidate" only).

**Sampled / time-boxed:**
- CONTRADICTION scan: checked key areas (stitching guidance, routing rules, delegation protocol,
  ARIA_OPS event-strip gap). Found one live contradiction (lesson vs skill on compliance stitcher).
  Did not do a full cross-product comparison of all rules × lessons × context docs.
- SUPERSEDED via git log: checked commits since 2026-06-13 (b2a7851, e8ba2e0, 5fd5bac, ad9b525)
  and grepped corpus for blocker/gap/queued/TODO language in the areas affected by those commits.
  Did not exhaustively read all 135+ context files for buried claims.
- DUPLICATION: clustered by topic (stitching, pak install, SDK build). The pak-install lesson
  cross-link recommendation from the prior report (DUPLICATION finding) was not applied; it
  recurs as a low-priority judgment item. Not re-flagged here to avoid noise — the prior report's
  entry stands.
- `knowledge/context/investigations/` — not exhaustively enumerated; treated as non-indexed (per README).
- `knowledge/context/reviews/` — treated as historical records; not cross-checked against current corpus.

**Classes with no new findings:**
- DEAD-REF (RULE-ID / agent-name / skill-name citations): none found.
- PROMPT-ROSTER SKEW beyond the 4 DRIFT agents: CLAUDE.md roster matches all agent prompts on
  spawn-when, posture, and model. `content-packager` skew was fixed in the prior cycle.
- New investigation/review files from PR #24: no path errors found in the new docs.
