# Curation report — 2026-06-13

Corpus scanned: rules/ (13 files) · lessons/ (19 files) · context/ (~130 files across
                root + mpb/ + authoring/ + api-surface/ + wire-formats/ + api-maps/ +
                reviews/ + investigations/ + other subdirs) · .claude/agents/ (20) ·
                skills (5) · .claude/commands/ (4)
Since: first run (no prior curation report)
Findings: 14  (7 mechanical / 7 judgment)

---

## Mechanical (orchestrator may apply as a small PR)

### [INDEX ROT] knowledge/lessons/INDEX.md — missing entry for `pak-uninstall-cascades-credentials.md`

`knowledge/lessons/pak-uninstall-cascades-credentials.md` exists on disk but has **no row** in
`knowledge/lessons/INDEX.md`. Every other lesson file in `lessons/` has a corresponding index row.

Evidence: `ls lessons/*.md` shows the file; `grep pak-uninstall-cascades knowledge/lessons/INDEX.md`
returns nothing.

Fix: Add a row to `knowledge/lessons/INDEX.md`:
```
| [pak-uninstall-cascades-credentials.md](pak-uninstall-cascades-credentials.md) | Pak uninstall on VCF Ops cascades into credential deletion — credentials return 404 after uninstall. Pre-flight: stash a credential manifest before DELETEing adapter instances; refuse if values aren't recoverable. |
```

---

### [INDEX ROT] knowledge/context/README.md — four disk files missing from the index

Four files exist on disk but are absent from `knowledge/context/README.md`:

| File | Location on disk | Why it matters |
|---|---|---|
| `mpb_import_investigation.md` | `knowledge/context/mpb/` | Referenced in context investigations but not indexed |
| `reference-mpb-research.md` | `knowledge/context/mpb/` | MPB JSON schema reference, cited by mp-author prompt |
| `view_dashboard_design_guide.md` | `knowledge/context/authoring/` | Content authoring guide with no index entry |
| `framework_v2_migration.md` | `context/` (root) | Cross-referenced 10+ times from `tier2_architecture.md` and reviews — the most significant gap |

Evidence: `ls knowledge/context/mpb/*.md` includes both mpb files; `ls knowledge/context/authoring/*.md`
includes `view_dashboard_design_guide.md`; `ls knowledge/context/framework_v2_migration.md` exists;
none appear in `knowledge/context/README.md` (`grep -l` returns nothing for each).

Note: `knowledge/context/managed_paks.md` and `knowledge/context/vrops-content-management.md` are also absent
from the README but are lower-priority (managed_paks is a registry document; vrops-content-
management.md is a vendor reference article imported from external source).

Fix: Add index entries in the appropriate README sections for the four files above.
`framework_v2_migration.md` belongs under "Read for MP tasks" (alongside `tier2_architecture.md`).

---

### [DRIFT] `.claude/agents/mp-author.md` — bare `managementpacks/` path throughout (6 occurrences)

`mp-author.md` consistently refers to the write target as `managementpacks/` (bare), but the
actual directory is `content/managementpacks/`. The CLI's DEFAULT_DIR is `content/managementpacks`
(`vcfops_managementpacks/cli.py:14`). There is no bare `managementpacks/` at the repo root.

This is exactly the failure mode documented in `knowledge/lessons/content-root-is-content-dir.md` ("stale
agent prompts named bare paths post-migration"). CLAUDE.md roster already shows the correct path
(`content/managementpacks/`) at line 79; the agent prompt was not updated to match.

Affected lines in `.claude/agents/mp-author.md`:
- Line 3 (frontmatter description): `"under managementpacks/"`
- Line 9: `` `managementpacks/`. Nothing else. ``
- Line 25: `existing managementpacks/*.yaml`
- Line 35: `**Write only under `managementpacks/`.**`
- Line 38: validate command: `validate managementpacks/<file>.yaml` (file is not there)
- Line 60–61: `Filename: managementpacks/<target_slug>.yaml`

Also affected: `knowledge/context/mpb/mpb_relationships.md:285` says
`"managementpacks/synology_nas.yaml"` (bare) in a worked example.

Fix: Replace all bare `managementpacks/` with `content/managementpacks/` in `mp-author.md`.
Update the worked example path in `mpb_relationships.md:285`. Both are straightforward
find-and-replace.

---

### [PROMPT-ROSTER SKEW] CLAUDE.md:75 — `content-packager` "Writes to" column says `dist/` only

CLAUDE.md roster (line 75): `content-packager | Build | dist/ | …`

`content-packager.md` frontmatter (line 3): `"Authors bundle manifest YAML under bundles/…"`
`content-packager.md` hard rule 2: `"Write manifests to bundles/."`

Reality: the agent writes YAML manifests to `bundles/` AND invokes the CLI to produce ZIPs in
`dist/`. CLAUDE.md only lists `dist/`, omitting `bundles/`, which is the agent's primary write
target for new manifests.

Fix: Update CLAUDE.md roster to `dist/, bundles/` (or similar) to reflect both write targets.

---

## Judgment (triage — Scott)

### [SUPERSEDED] `knowledge/lessons/stitch-moid-not-unique-across-vcenters.md:5` — "compliance fix queued"

Line 5 of the lesson: `**Same defect in the compliance adapter's stitcher — fix queued.**`
Line 73–74: `content/sdk-adapters/compliance/ — ComplianceStitcher carries the same defect;
fix queued. Until then it is NOT the pattern to copy for stitching cross-vCenter resources.`

The fix has landed. Evidence:
- `ComplianceStitcher.java` Javadoc (line 39): `"Cross-vCenter MOID scoping (the MOID trap —
  fix, build 51)."` — the full VMEntityVCID scoping pattern is implemented with `setOwningVcUuid()`.
- `context/session-handoff.md:49`: `"DEF-004 graduation candidate: compliance MOID fix is built
  (build 51, pushed c126194) but devel still runs build 50 — register per RULE-012 if unverified
  at next compliance review."`

The compliance fix is built and pushed to the pak's own repo. The "fix queued" language in the
lesson is superseded. The lesson's final paragraph should be updated: `ComplianceStitcher` now IS
the correct pattern to copy (as of build 51), subject to RULE-012 graduation.

Fix: Update lines 5 and 73–74 to reflect the landed fix. The lesson remains valuable for the
historical context; just retire the "fix queued / do NOT copy" warning.

---

### [STALE-FACT] `knowledge/context/tier2_architecture.md:396–400` — Phase 2 and Phase 3 lifecycle status

Lines 396–400:
```
- Phase 1 (framework + tooling skeleton): **COMPLETE**
- Phase 2 (Synology adapter): **IN PROGRESS** — build 1.0.0.7
  installed on devel, 22 objects discovered, 278 metrics collected.
  Pending: migration to v2 framework.
- Phase 3 (framework v2 migration — compliance, synology, unifi): IN PROGRESS.
  Framework v2 JAR built; adapters to be migrated serially by sdk-adapter-author.
```

Current reality:
- Synology is at **build 17** (`knowledge/context/reviews/synology-build-17.md`), not 1.0.0.7.
- `context/session-handoff.md:252–254`: `"synology/unifi v2 migrations DID land — synology
  build 14→16 and unifi build 3, both reviewed APPROVE"` (v2 framework migration complete for
  synology and unifi).
- Compliance is at build 51 with the framework v2 stitcher (VMEntityVCID fix landed).
- Phase 3 is functionally complete for all three adapters, though DEF-004 graduation for
  compliance is pending devel verification.

Note: The same stale boilerplate block was pasted into `knowledge/context/reviews/synology-build-17.md`
(lines 47–53). Review documents are historical records — do not edit them. The authoritative
fix is `tier2_architecture.md` only.

Fix: Update the "Implementation status" block in `knowledge/context/tier2_architecture.md` to reflect
current build numbers and phase completion. Judgment call: what "Phase 4" (if any) looks like
is Scott's call.

---

### [STALE-FACT] `knowledge/context/tier2_architecture.md:471` — "Adapter adoption for synology/unifi: both adapters must replace…"

Line 471 (changelog entry, 2026-06-10):
`"Adapter adoption for synology/unifi: both adapters must replace any shadow adapterLogger()
methods and all getAdapterLoggerFactory().getLogger(cls) call sites with componentLogger(cls)
before their v2 migration builds."`

Per session-handoff.md, the v2 migrations landed for both synology and unifi. This "must
replace" instruction is now obsolete (the adoption happened in build 14→16 and build 3).

Fix: This is a changelog entry — leave the historical text, but add a note indicating adoption
completed. Or annotate with `(adopted: synology build 16, unifi build 3)`.

---

### [STALE-FACT] `knowledge/context/tier2_architecture.md:471` — compliance `adapterLogger()` shadow claim

Same line 471: `"Compliance (build 46) has no framework-method shadow to remove — its
adapterLogger() shadow is in ComplianceAdapter.java and is the target of the pending compliance
v2 fixup."`

The compliance v2 fixup has since been built (session-handoff mentions builds 47–51 for
compliance). This claim is from the 2026-06-10 changelog and is now stale.

Fix: Same treatment as above — annotate with actual build when the fixup landed.

---

### [DUPLICATION] Pak install failure lessons — three overlapping docs, risk of silent divergence

Three lessons cover closely related territory — pak install structure failures:
- `knowledge/lessons/pak-install-reliability.md` (114 lines) — MPB format divergence, 5 root causes
- `knowledge/lessons/pak-content-bundling.md` (167 lines) — pak `content/` directory structure
- `knowledge/lessons/pak-content-localization-bundles.md` (99 lines) — localization bundles required

These are distinct root causes, not identical content. However, the `pak-content-bundling.md`
lesson mentions events (line 49: "SymptomSets needs ≥2 children") and `pak-install-reliability.md`
also covers adapter kind mismatch and CredentialKinds — there is meaningful overlap in the
"what can break a pak install" space.

The real risk: a new session searching "pak install failure" may read only one and miss the
others. The lessons do not cross-link each other.

Fix (judgment): Add a cross-link header to each lesson pointing to the other two. No content
changes needed. Example: `## See also` at the bottom of each with links to the sibling lessons.

---

### [CONTRADICTION / potential] `knowledge/lessons/content-root-is-content-dir.md:71` vs `mp-author.md` current state

`knowledge/lessons/content-root-is-content-dir.md:71` states:
`"CLAUDE.md roster 'Writes to' column corrected to content/<type>/"`

This implies the fix was applied. The CLAUDE.md roster IS correct (line 79: `content/managementpacks/`).
But the lesson implies the *agent prompts* were also fixed — they were not. `mp-author.md` still uses bare
`managementpacks/` throughout.

This is technically not a contradiction between two corpus rules (the lesson describes a past
correction, not a standing rule), but it creates a false impression that the drift was fully
resolved. This is better addressed as part of the DRIFT fix above.

---

## Coverage notes

**Fully scanned:**
- `knowledge/rules/INDEX.md` vs disk (all 13 rules match; no missing files, no missing index entries)
- `knowledge/lessons/INDEX.md` vs disk (1 missing entry found: `pak-uninstall-cascades-credentials.md`)
- `knowledge/context/README.md` vs disk for `mpb/`, `authoring/`, `api-surface/`, `wire-formats/`, and
  the context root (4 missing entries found; `api-surface/` entries fully match disk)
- All RULE-ID citations in the corpus (`grep -rn "RULE-0"` across all dirs) — no dangling old
  IDs found. The 471365e renumbering was cleaned up in 02071ff; subsequent additions (RULE-012,
  RULE-013) are consistently cited.
- All `.claude/agents/*.md` frontmatter + writes-to vs CLAUDE.md roster (PROMPT-ROSTER SKEW:
  1 found for content-packager; ops-recon, dashboard-author, sdk-adapter-reviewer, framework-
  reviewer, curator all match their roster entries)
- All skill name citations in agent prompts vs `.claude/skills/` on disk (all 5 match)
- `knowledge/designs/tier2-mp-architecture-plan.md` existence (confirmed present)
- `knowledge/context/investigations/v20-step5-silent-drop.md` existence (confirmed present)

**Sampled / time-boxed:**
- SUPERSEDED via `git log`: checked recent history (last 60 commits) and grepped for
  blocker/gap/TODO language. Found the compliance stitch "queued" claim. Did NOT exhaustively
  read all 130+ context files for buried TODO-style claims — high-value targets only.
- CONTRADICTION: checked key areas (delegation protocol, ops-recon spawn-when, RULE-011
  dashboard enforcement, pak events gap). No structural contradictions found. Did not do
  a full cross-product comparison of all rules × lessons × context docs.
- DUPLICATION: clustered by topic (stitching lessons, pak install lessons, SDK lessons). The
  three SSL/stitch/MOID lessons are distinct topics; the three pak-install lessons share
  territory but not content. No other high-confidence duplication candidates identified.
- knowledge/context/investigations/ — not fully enumerated; used only for cross-reference checking.
  Not part of the indexed corpus (README explicitly calls it "read when debugging").
- knowledge/context/reviews/ — treated as historical records; not indexed or cross-checked against
  current corpus. The boilerplate paste in `synology-build-17.md` noted but not flagged as
  a primary finding (review docs are intentionally immutable).
- `knowledge/context/authoring/view_dashboard_design_guide.md` content not read — flagged only for
  missing README entry.

**Classes with no findings:**
- DEAD-REF (RULE-ID / agent-name / skill-name citations): none found — renumbering cleanup
  was thorough; skill names are stable; agent names are consistent.
- CONTRADICTION (rules vs lessons vs context): no incompatible guidance found in the areas
  sampled. The pak events TOOLSET GAP is consistently documented across all three locations
  that mention it.
