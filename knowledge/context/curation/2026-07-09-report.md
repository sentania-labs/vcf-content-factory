# Curation report — 2026-07-09

Corpus scanned: rules/ (17, +INDEX.md) · lessons/ (22, +INDEX.md) ·
                context/ (330 files across top-level + subdirs) ·
                .claude/agents/ (20) · skills (5)
Since: 2026-07-02 (prior report)
Findings: 5  (0 mechanical / 5 judgment — but 2 are trivially mechanical
if Scott just says "yes, delete/reword")

**Context note:** the working tree is mid-reorg
(`chore/reorg-v2-phase1-src`) — the ten `vcfops_*` packages just moved to
`src/`, and a large uncommitted citation sweep is touching ~240 files
(mostly path-only edits, e.g. `vcfops_supermetrics/` → `src/vcfops_supermetrics/`
in prose). Per brief, in-flight reorg path churn is treated as expected
and not triaged item-by-item. I spot-checked that the sweep didn't
introduce *new* dangling `src/vcfops_*` references (it hasn't — the
touched files all resolve) and did not otherwise chase it. Only 14
commits landed since the last curation pass (`6cf273c`..`254cd9b`), so
this is a light cycle by design — most of the effort here is checking
whether last cycle's judgment findings were actioned.

## Mechanical (orchestrator may apply as a small PR)

None this pass. (The one candidate — adding `feedback_queue.md`,
`vrops-content-management.md`, `cleanroom-requests/` to
`knowledge/context/README.md` — was already handed to the orchestrator as
mechanical in the 2026-07-02 report and has not been applied yet; see
Judgment/repeat items below rather than duplicating it as "new.")

## Judgment (triage — Scott)

- **[REPEAT — INDEX ROT, now unaddressed for 2 cycles]**
  `knowledge/context/README.md` still has no entry for `knowledge/context/feedback_queue.md`,
  `knowledge/context/vrops-content-management.md`, or `knowledge/context/cleanroom-requests/`
  (verified on disk today; `grep -n "feedback_queue\|vrops-content-management\|cleanroom-requests" knowledge/context/README.md`
  returns nothing). Also newly noticed this pass: `knowledge/context/attic/`
  (added by the reorg, holds `legacy-root-content/` with 6 retired MSSQL/
  Oracle query-performance YAMLs) and `knowledge/context/reviews/` (33 build-review
  files + `framework/`) have **no** README section either — `reviews/` in
  particular is large and load-bearing (it's the `sdk-adapter-reviewer`
  / `framework-reviewer` write target cited directly in CLAUDE.md's
  roster) yet a reader following `knowledge/context/README.md` top-to-bottom would
  never discover it exists. → Same fix as last cycle, now scoped
  slightly wider: add rows/sections for `feedback_queue.md`,
  `vrops-content-management.md`, `cleanroom-requests/`, `attic/`, and
  `reviews/`. This was flagged mechanical last report and marked
  "orchestrator may apply" — it wasn't applied. Re-flagging as judgment
  this time only because the scope grew; the content of the fix is
  still mechanical.

- **[REPEAT — SUPERSEDED, now unaddressed for 3 cycles]**
  `context/session-handoff.md` (still headlined "Session Handoff —
  2026-06-14 ... ALL MERGED; vCommunity parked", still opens "No open
  PRs. vCommunity is still parked on a credential/log blocker only Scott
  can clear"). Last touched by a reorg path-rename commit (`46b1461`,
  2026-07-07) but the *content* is unchanged — still describes DEF-001–004
  only, predates DEF-005/006/007 (all closed since, per
  `knowledge/context/defects.md`), predates the entire unifi stitch-v2 saga
  (`context/reviews/unifi-build-8..11.md`, DEF-002/DEF-007 closures,
  `ce6ed62`), and predates the vcommunity three-adapter split landing
  (`9215736`). The file itself says "delete once consumed." Both prior
  reports (2026-06-29, 2026-07-02) recommended deletion; it is now 4+
  weeks past its self-declared relevance window and 2 full curation
  cycles past the deletion recommendation. → Delete it. If there's a
  reason it's being kept as an archival record, it should at minimum be
  moved out of the "read on demand" surface (it's not indexed in
  `knowledge/context/README.md` either, so it's currently orphaned in both
  directions — stale AND undiscoverable).

- **[REPEAT — STALE-FACT, now unaddressed for 3 cycles, self-referential]**
  `HOW_IT_WORKS.md:264-266` — "Tier 2 Phase 1 is complete... Synology and
  Dell PowerEdge are queued as the first real Tier 2 adapters" — and
  again at `HOW_IT_WORKS.md:400-402` under "Where the framework is
  going" — "Tier 2 Phase 2: Synology + Dell PowerEdge as the first real
  native-SDK adapters." Current reality per `knowledge/context/managed_paks.md`:
  the live Tier 2 roster is `synology`, `unifi`, `compliance`,
  `vcommunity`, `vcommunity-vsphere`, `vcommunity-os` — six adapters,
  synology alone at build 27+ with sustained PROD stitch proof (DEF-007
  closing evidence, 2026-07-06). Dell PowerEdge was tried and abandoned
  (`knowledge/lessons/dell-redfish-relationship-strategies.md`: "all three
  relationship strategies failed... requires Tier 2" — never actually
  built as Tier 2, isn't in the registry). This is, verbatim, the exact
  motivating STALE-FACT example quoted in `knowledge/designs/curator-v1.md:60-61`
  and `.claude/agents/curator.md`'s own hunt-list description — flagged
  in the 2026-07-02 report and still not fixed. → Reword both passages:
  drop "Dell PowerEdge," drop "queued"/"Phase 2 ... first real," replace
  with the current six-adapter roster and synology's shipped status.

- **[UNCHANGED — SUPERSEDED premise, still no correction note]**
  `knowledge/designs/sdk-adapters/synology-declarative-stitch-experiment.md:16-17`
  and `knowledge/designs/stitcher-identity-and-additive-foreign-v1.md:69` still
  assert, uncorrected, that Oracle's stitch was "zero adapter API calls"
  / "Oracle puzzle solved (describe.xml traversal, no suite-api calls)."
  This was disproven same-day back in early July and three sibling
  investigation docs got a dated CORRECTION addendum
  (`knowledge/context/investigations/recon_log.md`,
  `knowledge/context/investigations/cp-auth-door-probe-2026-07-01.md`,
  `knowledge/context/api-maps/tvs-datastore-binding-value.md`) — these two design
  docs never did. Flagged in the 2026-07-02 report; unaddressed. →
  Append the same short correction pointer used on the three sibling
  docs (real mechanism: per-cycle Suite API read under the
  platform-injected per-instance credential; see
  `knowledge/context/investigations/oracle-stitch-autopsy-2026-07-02.md`).

- **[NEW — DEAD-REF]** `knowledge/context/framework_v2_migration.md:795` cites
  `knowledge/lessons/unreadable-is-not-compliant.md` as a cross-reference ("Cross-
  reference: `knowledge/lessons/unreadable-is-not-compliant.md`") — **no such
  file exists** in `lessons/` (verified: `ls lessons/` has 22 entries,
  none matching; `comm` diff of every `lessons/*.md` citation in the
  corpus against the actual directory listing surfaces exactly this one
  dangling reference and no others). The concept is real and is used
  live — `.claude/agents/sdk-adapter-reviewer.md`'s description and
  hunt-list (`hunts the unreadable-is-compliant / stitch-corruption /
  crash-the-cycle failure modes`, "Cardinal correctness — 'unreadable is
  NOT compliant'") and several `context/reviews/compliance-build-*.md`
  reports reference the same rule by name — but it was apparently never
  codified as a standalone `lessons/` file, only as prose scattered
  across the reviewer prompt and skill. → Either (a) codify it as
  `knowledge/lessons/unreadable-is-not-compliant.md` per
  `knowledge/context/authoring/guide_codification.md` (it clearly qualifies — a
  named, cross-cited, hard-won rule with multiple build-review
  citations) and fix the one dangling cite, or (b) if it's deliberately
  staying prose-only in the reviewer prompt/skill, fix
  `framework_v2_migration.md:795`'s citation to point at the skill
  section instead of a nonexistent lesson file. This is the first
  concrete DEAD-REF finding this agent has produced against the
  citation-integrity class (prior cycles found none) — worth noting as
  the hunt list's fifth class finally producing a hit.

## Coverage notes

- **Fully scanned:** `rules/` vs `knowledge/rules/INDEX.md` (clean); `lessons/`
  vs `knowledge/lessons/INDEX.md` (clean, 22/22); every `RULE-0NN` citation in the
  corpus (all resolve to 001-017, no dangling IDs — knowledge/rules/INDEX.md's
  current numbering has no stray old references); every `lessons/*.md`
  and `rules/*.md` citation corpus-wide diffed against the actual
  directory listing (surfaced exactly the one DEAD-REF above, no rules
  dangling); the full 20-agent roster names cross-checked both
  directions against the CLAUDE.md table (exact match, no orphan agent
  files, no missing rows); each of the 20 agent prompts' `description`
  frontmatter spot-read against its CLAUDE.md roster row (`writes to` /
  `spawn when`) — no skew found, all consistent; `knowledge/context/defects.md`
  lifecycle (DEF-001 through DEF-007) cross-checked against every
  citing doc turned up by grep — no doc asserts a closed defect as
  still-open; the two SUPERSEDED design docs and the STALE-FACT/
  SUPERSEDED session-handoff item carried forward from 2026-07-02 (all
  three confirmed still unaddressed, not re-derived from scratch).
- **Sampled, not exhaustive:** DUPLICATION — only the previously-flagged
  stitch/SSL/MOID lesson cluster was re-checked (still clean, no new
  divergence since last cycle's CONTRADICTION fix); did not re-survey
  the wider corpus for new duplication clusters this pass, given the
  light 14-commit delta since 2026-07-02. The ~240-file uncommitted
  reorg/citation-sweep diff was spot-checked only for whether it
  introduces *new* dangling references (it does not) — not read in
  full; per the brief, in-flight reorg inconsistencies are out of scope
  for individual triage this cycle. `knowledge/context/reviews/` (33 files) was
  only surveyed for its INDEX ROT gap, not read for content staleness —
  these are timestamped point-in-time build records, not living docs,
  so SUPERSEDED doesn't apply to them the way it does to guidance docs.
- **No findings this pass:** CONTRADICTION (the one live contradiction
  from 2026-07-02 — `setrelationships-foreign-adapter-scoped.md` vs
  `cross-mp-stitch-cp-identity-and-edge-mechanics.md` — is now resolved:
  the older lesson carries a dated SUPERSEDED addendum pointing to the
  write-verb split); STALE-FACT beyond the two `HOW_IT_WORKS.md`
  passages (no new enumerable-count/version/status drift found; the
  "81 sections" example from the design doc's own interview transcript
  was already scrubbed from live docs); PROMPT-ROSTER SKEW (all 20
  prompts consistent with the CLAUDE.md roster); DRIFT (no moved/renamed
  paths found dangling outside the expected reorg churn —
  `content/managementpacks/` citations were checked and are legitimate:
  that's the still-current Tier 1 MPB YAML directory, distinct from
  Tier 2's `content/sdk-adapters/`, and `mp-author.md` / CLAUDE.md's
  roster row both still write there correctly).

## Summary for orchestrator

- Counts by class: INDEX ROT 1 (repeat, scope grew) · SUPERSEDED 2
  (both repeats) · STALE-FACT 1 (repeat) · DEAD-REF 1 (new).
- Mechanical/judgment split: 0 / 5 in form, but 4 of the 5 are repeats
  of already-triaged mechanical/low-judgment fixes from the 2026-07-02
  report that simply were not applied — the actual new judgment call
  this cycle is small (the DEAD-REF codify-or-repoint decision).
- **Highest-value finding:** not a new rot instance but a **process
  gap** — four of the five findings in this report are repeats of
  findings from the 2026-07-02 report (INDEX ROT, both SUPERSEDED items,
  the STALE-FACT) that were explicitly marked as candidates for a small
  follow-up PR or a quick edit and simply were not actioned across two
  full curation cycles. The `HOW_IT_WORKS.md` STALE-FACT is particularly
  notable: it is the literal example the curator agent's own design doc
  and prompt use to justify the agent's existence, and it has now
  survived three curation passes unfixed. If mechanical/low-risk
  curator findings aren't getting applied, the curator itself risks
  becoming exactly the kind of stale artifact it's meant to catch —
  recommend the orchestrator actually apply the small fixes (README
  index rows, the two HOW_IT_WORKS.md passages, deleting
  session-handoff.md, the two correction-note appends) this time rather
  than carrying them to a fourth cycle.
- Second-highest: the new DEAD-REF (`knowledge/lessons/unreadable-is-not-compliant.md`)
  — small blast radius but a clean, unambiguous hit on a hunt-list class
  that had produced zero findings in every prior cycle.
- Report path: `knowledge/context/curation/2026-07-09-report.md`.
