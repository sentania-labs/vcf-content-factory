# Curation report, 2026-09-23

Corpus scanned: knowledge/rules/ (18 + INDEX.md) . knowledge/lessons/ (31 + INDEX.md) .
                knowledge/context/ (README.md 191 lines, api-surface/ 32 files vs
                table, repo_layout.md, managed_paks.md, defects.md, known_limitations.md,
                targeted) . .claude/agents/ (20) . skills (5) . CLAUDE.md.
Since: 2026-09-09 (prior report). Busy window: roughly 150 commits, including a full
content-migrator project (M0 through v0.3.0, now split into its own repo per
`a1c68f0`), a new `vcfcf_core` shared library extracted from the per-type packages
(M2 rows 1 to 4), the `vcfops_*` to `vcfcf_*` import rename with one-release shims,
defect-registry gating work, and summary-dashboard tooling. Both 2026-09-09
carried-over findings (memory/*.md DEAD-REF, api-cartographer RULE-016
PROMPT-ROSTER SKEW) were closed in `70ffa71` and are confirmed fixed on disk;
not re-raised.

Findings: 4 (0 mechanical / 4 judgment).

## Mechanical (orchestrator may apply as a small PR)

None this cycle that are pure re-points. The DEAD-REF below is one line but
is a judgment call on wording, not a mechanical path swap, so it stays in
judgment.

## Judgment (triage, Scott)

- **[DEAD-REF]** `.claude/agents/curator.md:114` (this agent's own prompt)
  cites "RULE-007 applies to it: no em-dashes anywhere in it." `knowledge/rules/INDEX.md`
  shows RULE-007 is "UUIDs are part of the contract"
  (`knowledge/rules/uuid-stability.md`), unrelated to formatting. Grepped
  every file under `knowledge/rules/` for "dash": no repo RULE governs
  em-dashes at all. The no-em-dash requirement is rule 3 of the user's
  global `~/.claude/CLAUDE.md`, not a repo `RULE-XXX`. This citation has
  been wrong since the prompt was authored (git history of
  `knowledge/rules/INDEX.md` never shows RULE-007 as anything but UUIDs).
  Self-referential: every curation report to date, including this one, has
  been produced under a prompt that mis-cites its own formatting mandate.
  → Fix the sentence to drop the false rule-ID citation (e.g. "no
  em-dashes anywhere in it, per the user's global formatting rule" or
  simply state the requirement without a RULE-XXX cite) rather than
  point at a repo rule that doesn't carry it.

- **[PROMPT-ROSTER SKEW]** `.claude/agents/dashboard-author.md:9-11` and
  `.claude/agents/view-author.md:10-11` both now write to
  `third_party/<project>/dashboards|views/` when the orchestrator's brief
  names a project with a `PROJECT.yaml` (`factory_native: false`). Confirmed
  live: `third_party/idps-planner/` exists on disk. CLAUDE.md's roster table
  (lines ~101, ~98) still lists `dashboard-author` writing only to
  `content/dashboards/` and `view-author` only to `content/views/`, with no
  mention of the `third_party/` carve-out. → Add the carve-out to both
  roster rows, matching the wording already used for `api-explorer` /
  `api-cartographer`'s RULE-016 carve-out.

- **[STALE-FACT / DRIFT]** `CLAUDE.md`'s "Framework code" bullet (line 64)
  and `knowledge/context/repo_layout.md`'s "Python packages" section both
  describe `src/vcfcf_*/` as "per-type loaders, renderers, CLIs" with no
  mention of `src/vcfcf_core/`, which exists on disk and now holds the
  actual loader/render/packaging/extractor implementation for dashboards,
  customgroups, supermetrics, packaging and the extractor (moved there in
  M2 rows 1-4, per `knowledge/context/defects.md:1099-1101`: "moved into
  `vcfcf_core` in M2 row 4, old paths alias them"). Over 30 `knowledge/context/`
  docs now cite `src/vcfcf_core/...` paths directly (`view_column_wire_format.md`,
  `widget_renderer_scope.md`, `uuids_and_cross_references.md`, `defects.md`,
  etc.) but the two documents whose job is to orient a new reader to the
  package layout, `CLAUDE.md` itself and `repo_layout.md`, still describe
  the pre-M2 architecture where per-type packages hold their own
  loader/render code. `CLAUDE.md`'s own delegation-protocol section (lines
  317-318) already cites `src/vcfcf_core/packaging/assembly.py` and
  `src/vcfcf_core/dashboards/render.py` as tooling-change trigger paths,
  so the document contradicts its own overview bullet. → Add
  `vcfcf_core` to both the CLAUDE.md "Framework code" bullet and
  `repo_layout.md`'s package list, and correct the per-type package
  descriptions to say they wrap `vcfcf_core` rather than owning the
  loader/render logic directly. `tooling.md`'s "Package skeleton" section
  has the same gap (no `vcfcf_core` mention) though it is technically
  covered by the `src/vcfcf_*/` glob in its write-scope line, so lower
  priority than the two orientation docs.

- **[DRIFT, low severity]** `.claude/agents/tooling.md`'s "Package
  skeleton" example (`vcfcf_<type>/` with `loader.py`, `client.py`, etc.
  living directly in the per-type package) is the same pre-M2 shape as
  the STALE-FACT above, restated for the same reason. Bundled with the
  finding above since the fix is the same edit pass; listed separately
  because it is a different file.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (18/18, clean,
  no gap either direction, no renumbering since RULE-018 landed);
  `knowledge/lessons/*.md` vs `INDEX.md` (31/31, clean, three lessons
  added this window: `dashboard-import-without-views-corrupts-refs.md`,
  `deferred-nit-becomes-the-codex-finding.md`,
  `scoreboard-partial-bounds-render-unknown.md`, all present in INDEX.md
  with no orphans either direction); `knowledge/context/api-surface/`
  table in `knowledge/context/README.md` vs disk by full name-diff
  (32/32, zero gap either direction, including all new files this window:
  `2026-08-26-licensing-data-availability-9x.md`,
  `dashboard_import_two_phase_materialization.md`,
  `dashboard_widgets_alertvolume_section_viewdetails.md`,
  `summary_dashboard_pak_binding.md`, `view_multi_subject_column_binding.md`);
  full corpus md-reference sweep of `knowledge/context/README.md` against
  every file under `knowledge/context/`, `knowledge/rules/`, `knowledge/lessons/`
  (zero dangling references, zero orphaned files); `.claude/agents/` roster
  (20/20) cross-checked by name against CLAUDE.md's table, no additions
  or removals; `.claude/skills/` (5) unchanged. The two 2026-09-09
  carried-over findings re-verified closed on disk.
- **Targeted:** full diff of every `.claude/agents/*.md` and
  `.claude/skills/*/SKILL.md` against `70ffa71` (the last curated commit)
  to isolate real prompt changes from the mechanical `vcfops_` to `vcfcf_`
  rename; the rename itself introduces no new skew (both sides of every
  reference moved together). Grepped `knowledge/context/*.md`,
  `knowledge/rules/*.md`, `knowledge/lessons/*.md` for
  blocker/TODO/"in progress"/"not yet" language; every hit in
  `defects.md` and `known_limitations.md` inspected in context, and all
  read as dated log entries that already carry their own
  Progress/Closing-evidence resolution (not stale, self-updating format
  working as designed). `managed_paks.md` six-entry registry cross-checked
  against `content/sdk-adapters/` disk (6/6, `vcommunity` is mid-split per
  its own entry text, not yet stale since the retiring condition
  "vsphere+os reach devel parity" is not asserted as met).
- **Sampled:** DUPLICATION not re-swept exhaustively; spot-checked the
  known SSL/stitch cluster in `knowledge/lessons/INDEX.md`
  (`suite-api-stitch-ssl-tofu-vs-java-http.md`,
  `setrelationships-foreign-adapter-scoped.md` [already marked SUPERSEDED
  and cross-referenced], `stitch-moid-not-unique-across-vcenters.md`,
  `cross-mp-stitch-cp-identity-and-edge-mechanics.md`); no new overlap
  found among these four, but the three new lessons added this window
  were not checked against the full 31-lesson corpus for topical overlap.
  PROMPT-ROSTER SKEW checked by diffing every agent prompt against
  `70ffa71`; did not re-verify agents whose prompts were untouched this
  window against a fresh line-by-line CLAUDE.md read (last full 20-agent
  pass was 2026-08-26).
- **No findings:** CONTRADICTION (none surfaced this window; the
  `src/vcfcf_core` gap above is classed STALE-FACT/DRIFT, an omission and
  self-contradiction inside CLAUDE.md, rather than two docs asserting
  opposite guidance); SUPERSEDED (grepped for blocker/gap language against
  git log for this window, all resolved-in-place per the coverage note
  above; the migrator's design-doc move to its own repo in `a1c68f0` left
  a pointer rather than a dangling reference, checked directly); new
  INDEX ROT beyond the items already covered above.

## Summary for orchestrator

- Counts by class: DEAD-REF 1 (curator's own RULE-007 mis-citation) .
  PROMPT-ROSTER SKEW 1 (dashboard-author / view-author third_party/
  carve-out missing from CLAUDE.md, two roster rows) . STALE-FACT/DRIFT 2
  (CLAUDE.md + repo_layout.md missing `vcfcf_core`, plus tooling.md's
  matching package-skeleton example) . all other classes: zero new
  findings this window.
- Mechanical/judgment split: 0 mechanical / 4 judgment. All four are
  small, low-risk wording fixes (one to three sentences each) but are
  flagged judgment rather than mechanical because each requires picking
  the replacement wording, not a blind path re-point.
- **Highest-value finding:** the curator's own RULE-007 mis-citation
  (`.claude/agents/curator.md:114`). It is small in scope but
  structurally notable: this agent has been citing a wrong rule ID for
  its own core formatting mandate since the prompt was authored, and
  nothing caught it because the curator does not audit itself in the
  normal course of a run. Worth fixing promptly since it is a visible,
  literal instance of the exact CITATION-INTEGRITY class this agent
  exists to hunt.
- **Second-highest:** the `vcfcf_core` gap in `CLAUDE.md` and
  `repo_layout.md`. This is the more consequential finding for someone
  actually working in the repo: over 30 context docs now point engineers
  at `src/vcfcf_core/` for the real implementation, but the two
  orientation documents (`CLAUDE.md`'s overview and `repo_layout.md`)
  still describe the pre-M2 shape, and `CLAUDE.md` contradicts itself
  between its overview bullet and its later tooling-change trigger list.
  A `tooling` agent skimming just the overview would look in the wrong
  package.
- **Process note:** this was the largest commit volume between two
  curation cycles since tracking began (roughly 150 commits vs typical
  single digits to low dozens), driven by the content-migrator project
  and the `vcfcf_core` extraction. Despite the volume, both classes with
  historically the most churn (index rot, dead-refs) came back clean;
  the real drift this cycle was concentrated in the two "orientation"
  documents not keeping pace with a mid-cycle architecture change, which
  is exactly the STALE-FACT/DRIFT pattern this agent watches for.
- Report path: `knowledge/context/curation/2026-09-23-report.md`.
