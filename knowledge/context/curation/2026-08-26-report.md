# Curation report, 2026-08-26

Corpus scanned: knowledge/rules/ (18 + INDEX.md) · knowledge/lessons/ (28 + INDEX.md) ·
                knowledge/context/ (api-surface/ 27 files + managed_paks.md registry +
                defects.md + feedback_queue.md + known_limitations.md, targeted) ·
                .claude/agents/ (20, all touched this window) · skills (5)
Since: 2026-08-19 (prior report), a large window, ~38 commits, including a
full agent-prompt optimization pass (all 20 prompts touched), RULE-018
(POSIX-only) added then partially reverted-in-code, and bootstrap-v2
Phases 2+3 (credential wizard shipped).
Findings: 9 (3 mechanical / 6 judgment), 2 of the 3 mechanical findings from
2026-08-19 were applied in the window (`10f9326`, PR #98: api-surface index
rows + dead skill citation); a **new** INDEX ROT gap of different files was
found this cycle (see below). All 5 prior judgment findings are
unchanged/unresolved. One new judgment finding (SUPERSEDED, credential
wizard) is high-value: a CLAUDE.md instruction now directly contradicts the
doctor's own live guidance.

**Process note, the mechanical-fix loop is working again.** `10f9326`
(2026-08-20, PR #98) committed the missed 2026-08-19 report, reset the
staleness marker, added the two missing api-surface rows, and the dead
`vcfops-orchestration` skill citation was independently fixed in PR #87.
This breaks the "two consecutive cycles, zero findings applied" pattern
flagged 2026-08-19. However, the api-surface table drifted again
immediately: 3 different files (pre-existing since 2026-07-09, not new)
were never in the table and neither this fix nor any prior cycle caught
them, see INDEX ROT below. The "same commit" habit note added to
`knowledge/context/README.md` in `10f9326` did not close the actual gap
because the gap predates it.

## Mechanical (orchestrator may apply as a small PR)

- **[INDEX ROT]** ~~WITHDRAWN~~ **(retracted 2026-08-26 by non-author
  review, not applied.)** A reviewer re-ran the name-by-name disk-vs-table
  diff and found **27 files on disk, 27 rows in the table, zero
  discrepancy in either direction**. All three files named below are in
  fact present in `knowledge/context/README.md` at lines 136, 137 and 138.
  This finding is false, and the "the api-surface table drifted again
  immediately" process note in the header above is false with it. The
  finding text is preserved verbatim below as the honest record of what
  the curator reported, not as an open action item.

  ORIGINAL (FALSE) TEXT: `knowledge/context/README.md`'s `api-surface/` table
  vs disk: table lists candidate files but is missing 3 of 27 files on
  disk (`ls knowledge/context/api-surface/*.md`): `casa-injected-vs-raw-client.md`,
  `casa-inventory-access.md`, `per-instance-suiteapi-credential-contract.md`.
  These are not new, first committed `cfd5390` (2026-07-09, the reorg
  that moved context/ under `knowledge/`), so this is a distinct,
  older gap from the one fixed in `10f9326` (that one was
  `compliance_enablement_markers.md` / `resourcelist_column_state_wire_format.md`,
  both now correctly present). No curation report from 2026-06-13 through
  2026-08-19 caught these three; worth checking whether the prior cycles'
  count-based check (21 vs 23, etc.) was comparing against a stale disk
  snapshot or a narrower glob, since a name-by-name diff (`comm -23`
  disk-vs-table) is what surfaced them this cycle. → Add the 3 rows.

- **[SUPERSEDED, mechanical]** `CLAUDE.md:205-206` (First-run concierge,
  item 3, "Credentials"), "Until the credential wizard ships (bootstrap-v2
  Phase 2), guide the user to create `.env` from `.env.example` in their
  own editor; never ask them to paste a password into chat, and never
  echo one." The wizard **shipped** in `5bc8c5a` (2026-08-21, "bootstrap-v2
  Phases 2 + 3: credential wizard... #91 (#99)"): `src/vcfops_common/setup_credentials.py`
  (977 lines) exists, `python3 -m vcfops_common setup` is wired in
  `src/vcfops_common/__main__.py`, and `doctor.py` itself now instructs the
  orchestrator to route the user to it verbatim: "have the USER run the
  credential wizard themselves, in their own terminal (in a Claude session,
  typed with a leading `!`... `! python3 -m vcfops_common setup`. Never run
  it for them..." (`doctor.py:917-923`, `doctor.py:1054-1058`). CLAUDE.md's
  own concierge step 3 is now the single stale artifact contradicting its
  own doctor's live output, this is the closest match this cycle to the
  original worker.int/`0f7aad9` pattern the curator exists to catch:
  a fixed-state claim ("until X ships") still gating behavior after X
  shipped. Flagged mechanical (not judgment) because the fix is a
  same-meaning reword with a clear source of truth (`doctor.py`'s own
  text) rather than a call requiring Scott's judgment: replace item 3 with
  the wizard invocation instruction, matching doctor.py's phrasing
  (leading-`!`, no password in chat, TTY-gated).

- **[DEAD-REF]** carried over unchanged from 2026-08-19, 9th cycle, RULE-015
  violation, `knowledge/ROADMAP.md:84`, `knowledge/ROADMAP.md:138`,
  `knowledge/context/known_limitations.md:100` all still cite `memory/*.md`
  files that were never tracked (`git log --all` confirms none of the
  three paths were ever committed). → Per RULE-015: commit, inline-
  summarize, or drop.

## Judgment (triage, Scott)

- **[SUPERSEDED]** carried over unchanged, 4th+ cycle. Two locations, same
  stale claim, reconfirmed by fresh direct read of
  `content/sdk-adapters/compliance/src/com/vcfcf/adapters/compliance/ComplianceAdapter.java`
  (zero `adapterLogger()`, three `componentLogger(...)` call sites at
  lines 135, 148, 151):
  - `knowledge/context/framework_v2_migration.md:815-818` (§19),
    "**Compliance** still carries its historical private `adapterLogger()`
    shadow... is the target of the pending compliance v2 fixup."
  - `knowledge/context/tier2_architecture.md:505`, same claim in the
    2026-06-10 changelog row.
  Shadow removed build 49 (`eba91b2`); builds 50-51 landed since with no
  regression. → Update both; changelog row gets a footnote, not a rewrite.

- **[SUPERSEDED]** carried over unchanged, 4th+ cycle.
  `knowledge/context/feedback_queue.md:88-93` (FB-005) still reads
  "**Status:** open, root cause revised 2026-06-30... Fix in flight:
  `knowledge/designs/suite-api-stitcher-tls-auth-cleanup-v1.md`", that
  design doc's own status line (line 3) still says "DECIDED → in
  implementation." Both contradicted by `knowledge/context/defects.md`
  DEF-005, same defect, **Status: closed**, closing-evidence build 24,
  2026-07-02. Now **8 weeks** stale, survived at least 4 curation cycles,
  the longest-lived unresolved judgment finding in the corpus and still
  the closest analogue to the original `worker.int` pattern. → FB-005
  should read closed/resolved cross-referencing DEF-005; design doc status
  → implemented.

- **[CONTRADICTION]** carried over unchanged, 4th cycle.
  `knowledge/context/authoring/view_dashboard_design_guide.md:318-329`
  ("Dashboard layout conventions") still states "`y`: row (0-based, grows
  downward)" with an uncaveated `coords: {x: 0, y: 0, w: 12, h: 4}`
  example, while `knowledge/context/defects.md` DEF-013 (closed,
  2026-07-22) proved the wire format is 1-indexed and the renderer only
  floor-clamps as a backstop. This is the read-before-authoring guide,
  and this window shipped another dashboard
  (`knowledge/designs/dashboards/vm-snapshot-inventory.md`, referenced in
  `8ad7dd2` from the prior window), the incorrect guidance keeps
  circulating live authoring work. Still the single highest-value
  unresolved finding in this corpus, now 4 cycles / ~5 weeks old.

- **[DUPLICATION]** carried-over check, no change: spot-checked the
  SSL/stitch/MOID lesson cluster
  (`suite-api-stitch-ssl-tofu-vs-java-http.md`,
  `cross-mp-stitch-cp-identity-and-edge-mechanics.md`,
  `stitch-moid-not-unique-across-vcenters.md`) plus the 3 lessons added
  this window (`shared-helper-signature-change-needs-repo-wide-caller-sweep.md`,
  `no-subprocess-in-per-item-shell-loops.md`,
  `unenumerated-exit-status-is-not-a-verdict.md`) against the existing 25.
  No overlap: the new three are each a distinct incident (PR #137 caller
  sweep, #112 subprocess perf, the credential-wizard exit-status gap) with
  no existing lesson covering the same ground. No action.

- **[PROMPT-ROSTER SKEW]** carried over unchanged, 3rd cycle.
  `.claude/agents/api-cartographer.md`, despite the entire 20-agent
  optimization pass touching every prompt this window (`91a1c06`/`fddc873`,
  11 lines removed from this file specifically), Hard Rule #2 (line 26)
  still reads "Write only to `knowledge/context/api-maps/`. Never touch
  content YAML, `src/vcfops_*/` code, or `content/managementpacks/`" with
  no mention of the RULE-016 `reference/docs/` vendor-artifact carve-out
  that CLAUDE.md's roster row grants it (and that the sibling
  `api-explorer` prompt does state verbatim). This survived a pass whose
  explicit purpose was prompt correctness, worth flagging to whoever ran
  that pass that this specific line was in scope and missed. → Add the
  carve-out sentence, or narrow the CLAUDE.md roster row to match.

- **[STALE-FACT check, no finding]** Verified `knowledge/HOW_IT_WORKS.md:265`
  ("Tier 2 is in production: six adapters are registered") against
  `content/sdk-adapters/` (6 dirs: compliance, synology, unifi, vcommunity,
  vcommunity-os, vcommunity-vsphere) and `knowledge/context/managed_paks.md`
  (6 registry entries). Consistent, no drift despite the vcommunity
  three-way split landing since this claim was written. Noted as a
  negative result, not a finding.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (18/18, clean,
  bidirectional, RULE-018 addition correctly indexed); `knowledge/lessons/*.md`
  vs `INDEX.md` (28/28, clean, bidirectional, 3 new lessons this window
  correctly indexed); the 20-agent roster cross-checked by name against
  CLAUDE.md's table (exact match); `api-surface/` table vs disk by
  name-diff, not count (27 disk / 24 in-table before this report, 3-file
  gap found, see above); `managed_paks.md` registry (6 entries) vs
  `content/sdk-adapters/` disk (exact match) and `HOW_IT_WORKS.md`'s
  six-adapter claim (current); RULE-018 add (`c1cfc15`) and its later
  partial code-removal follow-up (`b0983e8`, reviewed/approved) checked
  for self-contradiction, none; both are a coherent add-then-cleanup
  pair, not rot.
- **Sampled:** DUPLICATION (existing cluster plus the 3 new lessons this
  window, no exhaustive topic-cluster sweep of all 28 lessons); DRIFT
  (grepped for `content/managementpacks/` vs `content/sdk-adapters/`
  cross-contamination, none found, all references correctly scoped to
  their tier); PROMPT-ROSTER SKEW (api-cartographer re-verified plus
  content-installer and content-packager spot-checked against their
  roster rows given the heavy packaging/installer commit volume this
  window, both consistent, no new skew); SUPERSEDED grep for
  blocker/TODO/"in flight"/"not yet" language across
  `knowledge/context/*.md`, `knowledge/rules/*.md`, `knowledge/lessons/*.md`
 , several hits (FB-011, vcommunity-os in-guest-collection blocker in
  `managed_paks.md`) read as still-genuinely-open historical/live entries
  on inspection, not flagged.
- **No findings:** CITATION-INTEGRITY/DEAD-REF beyond the carried-over
  RULE-015 `memory/` citations (rule-ID and lesson-name citations spot-
  checked across changed files this window, all resolve); new
  CONTRADICTION beyond the carried-over dashboard-coords one; new
  STALE-FACT beyond the six-adapter check above (explicitly verified,
  current).

## Summary for orchestrator

- Counts by class: INDEX ROT 1 **(retracted 2026-08-26, false: 27/27,
  zero gap; see the withdrawal note in the Mechanical section)** ·
  SUPERSEDED 3 (1 new: CLAUDE.md credential-wizard
  instruction, flagged mechanical; 2 carried over: compliance-shadow
  claim 4th+ cycle, FB-005/design-doc status now 8 weeks stale) ·
  DEAD-REF 1 (RULE-015 `memory/` citations, carried over 9th cycle) ·
  CONTRADICTION 1 (dashboard 0-based coords, carried over 4th cycle) ·
  PROMPT-ROSTER SKEW 1 (api-cartographer, carried over 3rd cycle,
  survived a full prompt-optimization pass) · DUPLICATION 0 new.
- Mechanical/judgment split: 3 mechanical / 6 judgment.
- **Highest-value finding:** the new SUPERSEDED item, CLAUDE.md's
  first-run concierge still instructs "guide the user to create `.env`
  manually... until the credential wizard ships," but the wizard shipped
  5 days into this window and the doctor's own live output already tells
  the orchestrator to route users to it. This is a governance doc telling
  the orchestrator to do the opposite of what the tool it governs now
  does, same shape as the original worker.int pattern, caught same-cycle
  rather than 6 weeks later.
- **Second-highest:** the dashboard-coords CONTRADICTION, now 4 cycles
  unresolved and still the corpus's longest-lived correctness-facing gap
  (FB-005 is longer-lived in calendar time but is a status label, not
  live authoring guidance someone would act on incorrectly today).
- **Process flag for the orchestrator:** the mechanical-fix loop worked
  this window (PR #98 applied 2/3 prior mechanical findings same-week).
  Worth extending that same discipline to the api-cartographer
  PROMPT-ROSTER SKEW, it is a single-line fix that has now missed three
  separate opportunities to be swept up, including a dedicated
  prompt-correctness pass.
- Report path: `knowledge/context/curation/2026-08-26-report.md`.
