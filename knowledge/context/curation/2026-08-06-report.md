# Curation report — 2026-08-06

Corpus scanned: knowledge/rules/ (17 + INDEX.md) · knowledge/lessons/ (25 + INDEX.md) ·
                knowledge/context/ (300 tracked `.md` files) ·
                .claude/agents/ (20) · skills (5)
Since: 2026-07-27 (prior report) — only **4 commits** landed in the window
(`9646289`, `7f34113`, `33c161b`, `716efd6`; the `builtin_metric_enables` /
DEF-016 close, a Peterson-comparison investigation note, the
vm-snapshot-inventory-dashboard 1.0 release, and a RULE-016 revert). None of
these four touch `knowledge/rules/`, `knowledge/lessons/`, `.claude/agents/`,
or skills.
Findings: 7  (2 mechanical / 5 judgment)

**Process note (not a hunt-list class, but load-bearing):** none of the
2026-07-27 report's findings were applied — checked each of the 6 directly.
Both mechanical items (api-surface/ index rows, the `vcfops-orchestration`
dead skill reference) and all 4 judgment items (compliance-shadow SUPERSEDED,
0-based-coords CONTRADICTION, the two RULE-015 `memory/` DEAD-REFs, the
api-cartographer PROMPT-ROSTER SKEW) are still present verbatim or
near-verbatim on disk today. This is the first cycle where a full carry-over
of zero fixes happened (2026-07-16's findings were applied by `2341d12`
before the 2026-07-27 cycle ran). Re-reported below rather than silently
dropped, since re-litigating unresolved findings is cheaper than losing them.
One item (the `vcfops-orchestration` skill citation) drifted slightly —
the surrounding sentence was reworded since 2026-07-27 but the dead name
itself was not fixed, so it is re-evidenced fresh, not just repeated.

## Mechanical (orchestrator may apply as a small PR)

- **[INDEX ROT]** `knowledge/context/README.md`'s `api-surface/` table
  (the `### \`api-surface/\`` section) lists **21** entries; disk has **23**
  files (`ls knowledge/context/api-surface/*.md`). Still missing, unchanged
  since 2026-07-27: `compliance_enablement_markers.md` (established
  2026-07-23, `6ac04b2`) and `resourcelist_column_state_wire_format.md`
  (established 2026-07-22, `9509915`). No new api-surface file landed this
  cycle — the miss is purely carried-over neglect, third cycle running for
  this exact pair (`api-surface/` also rotted in the 2026-07-16 report with
  a different set of missing rows). → Add the two rows. Given this is now a
  three-cycle-old debt on the same table, worth a standing habit note in
  `knowledge/context/README.md` itself ("update this table in the same
  commit that adds a file under `api-surface/`") rather than relying on
  curation to catch it every time.

- **[DEAD-REF]** `.claude/skills/vcfops-project-conventions/SKILL.md:10`
  (frontmatter `description`) — "For delegation protocol and workflow
  patterns, use vcfops-orchestration instead." No skill named
  `vcfops-orchestration` exists (`ls .claude/skills/` → 5 skills, none by
  this name; corpus-wide grep for the string returns only this one hit).
  Root cause unchanged from the 2026-07-27 finding: commit `8e4f13a`
  (2026-05-09) deleted `.claude/skills/vcfops-orchestration/SKILL.md` and
  folded its content into CLAUDE.md's delegation protocol — the deliberate,
  stated design. The sentence was lightly reworded since the last report
  (dropped the backtick formatting around the name) but the dangling
  reference itself was not fixed — now a 13-week-old stale citation that
  has survived 7 curation cycles. → Reword to "use CLAUDE.md's delegation
  protocol instead," or drop the sentence.

## Judgment (triage — Scott)

- **[SUPERSEDED]** Two locations, same stale claim, one broader than
  previously reported:
  - `knowledge/context/framework_v2_migration.md:815-818` (§19) — "Compliance
    still carries its historical private `adapterLogger()` shadow... target
    of the pending compliance v2 fixup." (Reported 2026-07-27, still
    unfixed.)
  - **New this cycle:** `knowledge/context/tier2_architecture.md:504` (the
    2026-06-10 changelog row for "componentLogger public accessor added") —
    "Compliance (build 46) has no framework-method shadow to remove — its
    `adapterLogger()` shadow is in `ComplianceAdapter.java` and **is the
    target of the pending compliance v2 fixup**." Same false claim, second
    surface.
  Both are contradicted by `content/sdk-adapters/compliance`'s own git
  history (build 49, commit `eba91b2`, "drop shadow logger") and by direct
  read of `ComplianceAdapter.java` today (zero `adapterLogger()` shadow;
  every acquisition goes through `componentLogger(...)`). Two further
  builds (50, 51) have landed since build 49 with no regression. → Update
  both passages; §19 can promote compliance to a second positive exemplar,
  and the tier2_architecture.md changelog row can get a one-line
  superseding note (changelog rows are historical record, so the fix there
  is a footnote/strikethrough, not a rewrite — treat differently from §19).

- **[SUPERSEDED]** `knowledge/context/feedback_queue.md:92` (FB-005,
  "Synology iSCSI/NFS → VMware Datastore cross-link never persists") —
  Status line reads "open — root cause revised 2026-06-30... Active prod
  blocker is the framework loopback Suite API transport... Fix in flight:
  `knowledge/designs/suite-api-stitcher-tls-auth-cleanup-v1.md`." That
  design doc's own status line (last touched 2026-07-09) still says
  "DECIDED → in implementation." Both are contradicted by
  `knowledge/context/defects.md` DEF-005 (same defect: "strict-TOFU
  loopback Suite API transport PKIX-fails every cycle — cross-MP stitch
  reads zero datastores," **Status: closed**, closing-evidence build 24,
  2026-07-02: "7+ consecutive clean cycles... zero PKIX," prod primary "10
  datastores loaded, 5 foreign Datastore edges live via API, no clobber")
  and by `knowledge/lessons/suite-api-stitch-ssl-tofu-vs-java-http.md`,
  which documents the finalized fix (`VcfCfAdapter.insecureSslContext()`,
  trust-all for the always-`localhost` Suite API hop) as generalized
  policy, not an in-flight patch. The design doc predates and was
  superseded by a second framework-reviewer round (round 1: CHANGES
  REQUESTED, `knowledge/context/reviews/framework/suiteapi-stitch-tls-auth-2026-06-30.md`,
  flagging `getAdapterTrustManager()` as wrong; the eventual landed fix used
  `insecureSslContext()` instead — a different call than either the design
  doc or the round-1 review discuss, evidence the design doc's body is
  stale, not just its status line). → FB-005 should read `closed` (or
  `resolved`, cross-referencing DEF-005) and the design doc's status line
  should read `implemented` with a closing pointer to DEF-005 and the
  lesson. This one predates the 2026-07-27 baseline (root cause landed
  2026-07-02) and slipped past at least one prior curation cycle — worth
  noting since `feedback_queue.md` entries are exactly the kind of
  "resolved but still says open" claim this curator exists to catch.

- **[CONTRADICTION]** `knowledge/context/authoring/view_dashboard_design_guide.md:318-329`
  ("Dashboard layout conventions" / "Grid system") vs `knowledge/context/defects.md`
  DEF-013 (closed) + follow-up FB-017 (open). Unchanged since 2026-07-27:
  the guide still states "`y`: row (0-based, grows downward)" with a worked
  example `coords: {x: 0, y: 0, w: 12, h: 4}    # full-width, 4 rows tall`
  and no caveat, while DEF-013 proved the wire format is 1-indexed
  ("Every widget coordinate in the known-good corpus... uses `x >= 1` and
  `y >= 1`; zero use 0 for either axis") and recommends 1-based authoring
  going forward. This is the read-before-authoring doc; still the
  highest-value unresolved finding across the last two cycles. → Update
  lines 320-329 to 1-based coords with a callout citing DEF-013's
  floor-clamp as a backstop, not the spec.

- **[DEAD-REF]** (RULE-015 violation, unchanged) — two durable governance
  docs still cite `memory/` files that are `.gitignore`d
  (`memory/sessions/`, `memory/environment/` per `.gitignore:52,62`) and
  were never committed to this repo (`git log --all` confirms):
  - `knowledge/ROADMAP.md:84-85` — "(see `memory/feedback_powershell_idioms.md`)"
  - `knowledge/ROADMAP.md:138` — "manual-UI MPB tasks tracked under
    `memory/project_mpb_manual_ui_tasks_pending.md`"
  - `knowledge/context/known_limitations.md:100` — "See
    `memory/project_vcf_ops_902_ui_deadends.md`"
  (The third instance flagged 2026-07-27 at `ROADMAP.md:84-85` is one
  citation, not two — confirmed exact line range unchanged.) → Per RULE-015
  (`knowledge/rules/cited-artifacts-reproducible.md`): commit the content if
  still relevant, inline-summarize, or drop the sentence.

- **[PROMPT-ROSTER SKEW]** `.claude/agents/api-cartographer.md` vs
  CLAUDE.md roster row — unchanged. CLAUDE.md grants api-cartographer a
  `reference/docs/` RULE-016 write carve-out ("vendor artifacts may be
  added under `reference/docs/`"); the agent's own frontmatter and Hard
  Rule #2 still say only "Write only to `knowledge/context/api-maps/`,"
  with no RULE-016 mention (contrast the sibling `api-explorer`, whose
  prompt does state the carve-out verbatim). → Reconcile: either add the
  carve-out to the prompt or narrow the roster row.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (17/17, clean,
  bidirectional check — no filename absent either direction); `knowledge/lessons/*.md`
  vs `INDEX.md` (25/25, clean, bidirectional); the full 20-agent roster
  cross-checked row-count against CLAUDE.md's table (20/20); the 5 skill
  frontmatters read in full (1 dead cross-reference, reported); the
  `api-surface/` README table vs disk (21/23, same 2-file gap as last
  cycle); the DEF-016/DEF-017 entries added this cycle cross-checked
  against the citing commit (`9646289`) and the referenced framework-review
  report (`knowledge/context/reviews/framework/packaging-discrete-builtin-metric-enables-2026-07-27.md`,
  exists, content matches the defects.md summary — no drift); the
  `managed_paks.md` 6-entry registry vs `content/sdk-adapters/` on disk
  (exact match) and `HOW_IT_WORKS.md`'s "six adapters are registered"
  claim (current, no STALE-FACT); re-verified all 6 unresolved findings
  from the 2026-07-27 report by direct read of current file state (not
  assumed carried-over) — one (`vcfops-orchestration`) had a partial,
  non-substantive edit since, the other 5 are byte-for-byte unchanged.
- **Sampled, not exhaustive:** the working-tree uncommitted diff (
  `content/dashboards/fleet_capacity_rightsizing.yaml`,
  `content/views/oversized_vms_reclaimable.yaml`,
  `content/views/undersized_vms_add_resources.yaml`, and their
  `knowledge/designs/` counterparts) is in-flight content authoring, out of
  this curator's scope (content/ and per-object design notes aren't part of
  the governance corpus this agent audits) — not evaluated. Given the
  4-commit delta since 2026-07-27, DUPLICATION got only a spot-check
  (re-confirmed the SSL/stitch/MOID lesson cluster is still non-overlapping;
  no new investigations/reviews landed this cycle to cluster). STALE-FACT
  was scoped to the same enumerable claims checked last cycle (adapter
  count, corpus-size language) plus the two new DEF entries — no drift
  found beyond the two SUPERSEDED items above (both are lifecycle-status
  claims, reported as SUPERSEDED per the design doc's own convention, not
  double-counted as STALE-FACT).
- **No findings:** DRIFT (none this cycle; no path moves/renames landed in
  the 4-commit window); INDEX ROT beyond the carried-over api-surface gap
  (rules/lessons indexes both clean); new PROMPT-ROSTER SKEW (no new/edited
  agent prompts this cycle to check beyond the carried-over
  api-cartographer item).

## Summary for orchestrator

- Counts by class: INDEX ROT 1 (carried over) · DEAD-REF 2 (1 skill
  citation carried over, 1 RULE-015 violation — 3 citations across 2 files
  — carried over) · SUPERSEDED 2 (compliance-shadow claim, now 2 surfaces;
  FB-005/design-doc status, new finding) · CONTRADICTION 1 (dashboard
  0-based coords, carried over) · PROMPT-ROSTER SKEW 1 (api-cartographer,
  carried over).
- Mechanical/judgment split: 2 mechanical / 5 judgment.
- **Highest-value finding:** still the CONTRADICTION —
  `knowledge/context/authoring/view_dashboard_design_guide.md`'s 0-based
  coords guidance, now unresolved across **two full curation cycles**
  (first flagged 2026-07-27). It is read *before* authoring, so it keeps
  steering the next dashboard author into the exact bug DEF-013 already
  spent a Playwright pass and a renderer patch fixing.
- **Second-highest / new this cycle:** the FB-005 SUPERSEDED finding —
  `feedback_queue.md` and a design doc both still describe a fix as
  "in flight" that closed over a month ago (DEF-005, 2026-07-02) and is now
  documented as finalized, generalized policy in a lesson file. This is
  exactly the `worker.int`/`0f7aad9` pattern the curator was built to catch
  — a resolved-blocker claim that outlived its fix by five weeks, still
  circulating.
- **Process flag for the orchestrator:** zero of the 6 findings from
  2026-07-27 were applied before this cycle ran. If mechanical findings in
  particular keep going unapplied, the "orchestrator may apply as a small
  PR" half of this agent's contract is not being exercised — worth a
  one-line check-in with Scott on whether that follow-up PR step is
  happening.
- Report path: `knowledge/context/curation/2026-08-06-report.md`.
