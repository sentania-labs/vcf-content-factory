# Curation report, 2026-09-09

Corpus scanned: knowledge/rules/ (19 + INDEX.md) . knowledge/lessons/ (28 + INDEX.md) .
                knowledge/context/ (README.md 185 lines, api-surface/ 28 files vs
                table, feedback_queue.md, defects.md, managed_paks.md, targeted) .
                .claude/agents/ (20) . skills (5) . CLAUDE.md.
Since: 2026-08-26 (prior report). Unusually quiet window: only two commits
landed after the prior report's own apply-commit pair (938f8a8, 7b9dd5a),
both dated 2026-08-25 and already ancestors of that pair in the branch
history (compliance SCG-only benchmark recon/intent-capture work). No
`.claude/agents/`, `.claude/skills/`, or `knowledge/rules/` changes since.
Three uncommitted items on the current branch (`knowledge/designs/defect-isolation-v1.md`,
`knowledge/context/reviews/compliance-scg-benchmark-set-2026-08-25.md`,
`knowledge/context/api-surface/2026-08-26-licensing-data-availability-9x.md`,
plus the matching README.md table row and a recon_log.md append) were
treated as current state per brief, not audited as drift; their internal
citations were spot-checked and resolve (RULE-012, RULE-016,
`knowledge/lessons/unenumerated-exit-status-is-not-a-verdict.md` all exist).

Findings: 2 (0 mechanical / 2 judgment). Both are carried over verbatim
from 2026-08-26, unresolved, because nothing touched their target files
this window. All 2026-08-26 findings that were applied (credential-wizard
SUPERSEDED, dashboard-coords CONTRADICTION, compliance-shadow SUPERSEDED,
FB-005 SUPERSEDED, the retracted INDEX ROT) are confirmed fixed and not
re-raised. No new findings surfaced.

## Mechanical (orchestrator may apply as a small PR)

None this cycle. The one carried-over mechanical item from 2026-08-19/26
(RULE-015 `memory/*.md` dead citations) is reclassified below as it has
now survived enough cycles unactioned that it needs a judgment call on
disposition, not just a mechanical re-point (see DEAD-REF below).

## Judgment (triage, Scott)

- **[DEAD-REF]** carried over unchanged, 10th cycle, RULE-015 violation.
  `knowledge/ROADMAP.md:84`, `knowledge/ROADMAP.md:138`,
  `knowledge/context/known_limitations.md:100` all still cite `memory/*.md`
  files (`memory/vcfcf_p0_acceptance_bugs...`, `memory/project_mpb_manual_ui_tasks_pending.md`,
  `memory/project_vcf_ops_902_ui_deadends.md`) that were never tracked
  (`git log --all -- 'memory/*'` returns nothing for any of the three
  paths). This is the longest-lived open finding in the curation history,
  now spanning 10 reports since 2026-06-13-equivalent tracking began and
  well past the point where "carry forward" is doing any work. Per
  RULE-015 the choices are commit the file, inline-summarize the content
  into the citing doc, or drop the reference. None of the three has
  happened across 10 cycles. Recommend picking one this time rather than
  a further carry-forward: if the underlying `memory/*.md` content no
  longer exists anywhere (not just untracked, actually gone), the
  citations should be dropped or replaced with an inline one-line
  summary, since a dangling citation to content nobody can find is worse
  than no citation.

- **[PROMPT-ROSTER SKEW]** carried over unchanged, 4th cycle.
  `.claude/agents/api-cartographer.md:27`, Hard Rule #2, still reads
  "Write only to `knowledge/context/api-maps/`. Never touch content YAML,
  `src/vcfops_*/` code, or `content/managementpacks/`" with no mention of
  the RULE-016 `reference/docs/` vendor-artifact carve-out that CLAUDE.md's
  roster row grants it ("vendor artifacts may be *added* under
  `reference/docs/` (RULE-016)"). Confirmed via `git diff --stat
  938f8a8..HEAD -- .claude/agents/` that this file was not touched this
  window (only `curator.md` changed, for the RULE-007 note), so the gap
  persists unchanged. This is now the second-longest-lived open finding
  and has survived a dedicated full prompt-optimization pass
  (91a1c06/fddc873) plus three subsequent curation cycles without
  resolution. It is a one-line fix (add the carve-out sentence, matching
  the sibling `api-explorer` prompt's wording).

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (19/19, clean,
  no gap either direction); `knowledge/lessons/*.md` vs `INDEX.md` (28/28,
  clean, no gap either direction, no lessons added or removed this
  window); `api-surface/` table in `knowledge/context/README.md` vs disk
  by full name-diff (28 files on disk, 28 rows in table, zero gap either
  direction, including the new `2026-08-26-licensing-data-availability-9x.md`
  row already present in the working tree); `.claude/agents/` roster
  (20/20) cross-checked by name against CLAUDE.md's table, no additions
  or removals; `.claude/skills/` (5) unchanged; `HOW_IT_WORKS.md`'s
  "six adapters registered" claim re-checked against `content/sdk-adapters/`
  and `managed_paks.md`, still current, no drift.
- **Targeted:** grepped `knowledge/context/*.md`, `knowledge/rules/*.md`,
  `knowledge/lessons/*.md` for blocker/TODO/"in flight"/"still open"
  language (12 hits); each inspected in context (`feedback_queue.md`
  FB-010, FB-011; `defects.md` DEF-005 residual, DEF-006 candidate fixes,
  a reverted 2026-07-12 closure attempt; `managed_paks.md` KNOWN-OPEN
  item; two lesson self-references to "still-open blocker" as worked
  examples, not live status claims). All read as genuinely open, dated,
  and internally consistent with `defects.md`/`feedback_queue.md`
  cross-references; none contradicted by any commit in this window since
  no relevant commits landed. No new SUPERSEDED findings.
- **Sampled:** DUPLICATION not re-swept exhaustively this cycle since
  zero lessons were added or removed since the 2026-08-26 full sweep;
  no new candidate cluster to check. PROMPT-ROSTER SKEW re-verified only
  the known api-cartographer gap plus confirmed via diff that no other
  agent prompt changed this window, so no new skew is possible; did not
  re-run a full 20-agent line-by-line diff (last done 2026-08-26,
  unchanged corpus since).
- **No findings:** CONTRADICTION (the dashboard-coords one closed
  2026-08-26 and confirmed fixed by reading the corrected
  `view_dashboard_design_guide.md` coordinate example and
  `widget_renderer_scope.md`'s five corrected examples); new DRIFT (no
  path moves this window, nothing to check); new STALE-FACT beyond the
  six-adapter re-verification above; new CITATION-INTEGRITY beyond the
  carried-over `memory/` DEAD-REF; the three new/uncommitted docs
  (`defect-isolation-v1.md`, the compliance-SCG review, the licensing
  api-surface file) spot-checked for dangling internal citations
  (RULE-012, RULE-016, the exit-status lesson), all resolve.

## Summary for orchestrator

- Counts by class: DEAD-REF 1 (RULE-015 `memory/` citations, now 10th
  consecutive cycle unresolved) . PROMPT-ROSTER SKEW 1 (api-cartographer
  RULE-016 carve-out, 4th cycle unresolved) . all other classes: zero new
  findings, prior findings confirmed closed.
- Mechanical/judgment split: 0 mechanical / 2 judgment. Both carried-over
  items are technically one-line fixes but are flagged judgment because
  they have already been offered as mechanical fixes multiple times
  without being picked up, so the open question for Scott is process
  ("why do these two specific items keep getting skipped") more than
  content.
- **Highest-value finding:** the `memory/*.md` DEAD-REF, now the single
  longest-lived unresolved item across all 10 curation reports to date.
  It has outlasted every other finding including the 8-week FB-005 case
  that was finally closed in 938f8a8. Worth a direct decision this cycle
  (commit/summarize/drop) rather than another carry-forward, since the
  finding itself is not in dispute, only the disposition.
- **Second-highest:** api-cartographer's missing RULE-016 carve-out
  sentence, unresolved for 4 cycles including one dedicated prompt-pass
  that should have caught it. Trivial fix, repeatedly not applied.
- **Process note:** this window had almost no corpus churn (2 commits,
  both pre-dating the prior report's own apply-commit in the branch
  timeline, plus 3 uncommitted docs treated as current state per brief).
  The two open findings are unchanged not because they were re-verified
  against new evidence but because nothing moved; worth noting for
  trend-reading that "unchanged" this cycle means "untouched," not
  "re-confirmed against fresh commits."
- Report path: `knowledge/context/curation/2026-09-09-report.md`.
