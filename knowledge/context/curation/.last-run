# Curation staleness marker (COMMITTED). Read by scripts/curation_staleness_check.sh.
# last_run = ISO8601 timestamp of the last completed curation pass.
# The orchestrator resets this to "now" when the curator completes.
# (The per-checkout session counter lives in .sessions-since, which is gitignored.)
last_run=2026-07-09T00:00:00Z
