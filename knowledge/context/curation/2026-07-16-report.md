# Curation report — 2026-07-16

Corpus scanned: knowledge/rules/ (17 + INDEX.md) · knowledge/lessons/ (23 + INDEX.md) ·
                knowledge/context/ (283 tracked `.md` files across top-level + 15 subdirs) ·
                .claude/agents/ (20) · skills (5)
Since: 2026-07-09 (prior report; all 5 of that report's findings were applied in
`e0c1de8` "chore(curation): apply all five 2026-07-09 curator findings (#42)" —
verified below, first curation pass to see its own output fully actioned)
Findings: 5  (3 mechanical / 2 judgment)

**Context note:** two reorg PRs landed since the last pass —
`30b025e` (reorg-v2 phase 1: `vcfops_*` → `src/`) and `cfd5390` (reorg-v2
phase 2: `rules/`, `lessons/`, `context/`, `designs/` → `knowledge/`). Both
were exhaustively swept per their own closeout commits
(`1cd6f61`, `4b171e2`) and the corpus is largely clean of both moves — I
verified this rather than assuming it, and found two straggler rule files
the phase-2 sweep missed (below). 21 commits landed since the last curation
baseline, including five defect closures (DEF-008/009/010(open)/011/012(open)).

## Mechanical (orchestrator may apply as a small PR)

- **[DRIFT]** `knowledge/rules/reference-immutable.md:1,14` and
  `knowledge/rules/distilled-doc-provenance.md:12,20,22` still use the
  pre-reorg bare-path shorthand `` `context/` `` / `` `lessons/` `` —
  these are the **only two** files in `knowledge/rules/` that weren't
  updated when reorg-v2 phase 2 (`cfd5390`, 2026-07-13) moved those
  directories under `knowledge/`. Every other rule file in the corpus
  already uses the qualified `knowledge/context/...` / `knowledge/lessons/...`
  form (verified: `grep -rn 'context/\|lessons/\|rules/' knowledge/rules/*.md`
  returns clean everywhere else, and `knowledge/rules/INDEX.md`'s own
  RULE-016 one-line summary — `` corrections live in `context/` `` — has
  the same drift). Likely cause: RULE-016/017 were the two newest rules
  and may have been mid-flight or added right around the phase-2 cut.
  → Reword the 5 occurrences (2 files + 1 INDEX.md summary line) to
  `knowledge/context/` / `knowledge/lessons/`. Pure path-prefix fix, no
  semantic change.

- **[INDEX ROT]** `knowledge/context/README.md`'s `api-surface/` table
  (lines ~101–113) lists 14 files; disk has 20
  (`ls knowledge/context/api-surface/*.md`). Six on disk, absent from the
  index: `casa-injected-vs-raw-client.md`, `casa-inventory-access.md`,
  `dashboard_selfprovider_pin_wire_format.md`, `distribution_view_no_data.md`,
  `per-instance-suiteapi-credential-contract.md`,
  `view_render_internal_endpoint.md`. Two of these are load-bearing and
  recent: `distribution_view_no_data.md` is DEF-012's cited root-cause doc
  (open blocking defect) and `dashboard_selfprovider_pin_wire_format.md` is
  the wire-format reference for the PR #54 dashboard-pin fix — a reader
  following the README top-to-bottom would not discover either. → Add the
  six rows to the `api-surface/` table.

- **[INDEX ROT]** `knowledge/context/README.md`'s `wire-formats/` table
  (lines ~52–58) lists 7 files; disk has 9
  (`ls knowledge/context/wire-formats/*.md`). Missing:
  `alertdef_symptomset_import.md`, `symptomdef_severity_import.md`. →
  Add the two rows.

## Judgment (triage — Scott)

- **[SUPERSEDED]** `knowledge/context/session-handoff.md` (untracked,
  headlined "2026-07-12 — vcommunity-vsphere parity closeout endgame",
  self-declared "delete once consumed... lives in the working tree only").
  Every substantive claim in it is now resolved: "Open PRs (factory) —
  PR #50 ... PR #51" — both merged (`3439502`, `5b61f2d`); "DEF-009 is
  OPEN in this PR" — closed (`b65f0b8`, "close DEF-009 — buildkit-1.0.9
  tarball build + live four-tier proof"); "Published sdk-buildkit-1.0.8 is
  broken... FIXED, reviewed... PR #52 open" — merged (`6c664dc`). This is
  the *second* handoff doc this agent has flagged as consumed-but-not-deleted
  (the first, a **tracked** file with the same header pattern, was flagged
  SUPERSEDED for 3 straight cycles — 2026-06-29/07-02/07-09 — before
  finally being deleted in the 2026-07-09 batch fix). This one is
  untracked so it costs nothing to leave (no dirty-tree/PR concern), but
  it is a governance-corpus doc under `knowledge/context/` all the same
  and every claim in it has aged out in 4 days. → Delete it, but see the
  next finding first — one backlog item inside it has no other home.

- **[CITATION-INTEGRITY]** `knowledge/context/defects.md`'s DEF-011 entry
  ("Related" field) cites `` session-handoff backlog (`version_line_guard`
  pre-push hook, now with a concrete incident behind it) `` as if it were
  a durable pointer. The cited backlog item — a pre-push hook to catch
  the exact `0.x`-tagged-as-`1.x` incident DEF-011 just closed — exists
  **only** inside `knowledge/context/session-handoff.md` (line ~139: "unifi
  build 12 ...; optional version_line_guard pre-push hook; annotated dead
  citation in extractor.py; stale memory/ citation in
  vcfops_dashboards/packager.py"), which is untracked, self-declared
  transient, and flagged SUPERSEDED above. `knowledge/context/feedback_queue.md`
  (the durable intake queue for exactly this kind of enhancement request)
  does **not** contain it — checked directly, no match for
  `version_line_guard`. If `session-handoff.md` is deleted per the finding
  above (as its predecessor was), DEF-011's own cross-reference to the
  concrete, incident-backed fix for the class of bug it documents becomes
  a dead reference with no recovery path. → Before deleting
  `session-handoff.md`, migrate the `version_line_guard` pre-push-hook
  item (and, if in scope for this agent's remit, the two adjacent stale-code-citation
  items — "annotated dead citation in extractor.py," "stale memory/ citation
  in vcfops_dashboards/packager.py" — flagged only as a pointer, not
  re-derived here) to `knowledge/context/feedback_queue.md`, then delete.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `knowledge/rules/INDEX.md`
  (17/17, clean); `knowledge/lessons/*.md` vs `knowledge/lessons/INDEX.md`
  (23/23, clean); every `RULE-\d+` citation corpus-wide against the 17
  defined IDs (no dangling numbers — the one apparent "RULE-0" hit was a
  false positive from prior curation reports' own grep patterns, not a
  real citation); the full 20-agent roster cross-checked both directions
  against the CLAUDE.md table (exact match); every agent's frontmatter
  `description` spot-read against its roster row plus a body-level check
  on `api-cartographer` (writes to `knowledge/context/api-maps/` — correct,
  just not restated in the frontmatter one-liner) and `content-installer`
  (Playwright visual-verification section present, matches CLAUDE.md's
  "Package + QA" workflow note) — no skew found; every itemized README
  subdirectory table (`mpb/`, `wire-formats/`, `authoring/`, `api-surface/`)
  diffed file-by-file against disk (`mpb/` and `authoring/` clean 22/22 and
  18/18; `wire-formats/` and `api-surface/` both rotted, reported above);
  DEF-008 through DEF-012 lifecycle statuses (`defects.md`) cross-checked
  against every citing doc and the git log commits that claim to close
  them — no doc asserts a closed defect as still open, and DEF-012's three
  recent commits (guard hardening) correctly leave it `open` since they
  fix the validate-time warning, not the shipped pak's views; RULE-014
  (`pak-version-lines.md`) checked against DEF-011's incident narrative for
  contradiction — none, the rule and the incident agree; the three
  SSL/stitch/MOID-adjacent lessons (`cross-mp-stitch-cp-identity-and-edge-mechanics.md`,
  `stitch-moid-not-unique-across-vcenters.md`,
  `suite-api-stitch-ssl-tofu-vs-java-http.md`) re-read for DUPLICATION —
  still three genuinely distinct topics (ambient identity/edge formation,
  MOID vCenter-scoping, TLS transport), no overlap; enumerable STALE-FACT
  candidates (corpus size claims in `CLAUDE.md`/`curator.md`, adapter-roster
  counts in `knowledge/HOW_IT_WORKS.md`, `managed_paks.md`'s six entries) all
  checked and current.
- **Sampled, not exhaustive:** the prose-only (non-itemized) README
  subsections — `api-maps/` (17 files), `cleanroom-spec/` (6),
  `specimens/` (5), `adapter_describe_cache/` (3), `exports/` (7),
  `investigations/` (56), `cleanroom-requests/` (4), `reviews/` (75) —
  were only confirmed to exist and be non-empty, not read for individual
  content staleness; these are correctly described in prose ("one file per
  target," "timestamped records, not living docs") rather than itemized,
  so per-file INDEX ROT doesn't apply the way it does to the four itemized
  tables above. DUPLICATION was scoped to the one previously-known cluster
  (SSL/stitch/MOID) plus a fresh check on the DEF-012-adjacent distribution-view
  docs (clean, single source `distribution_view_no_data.md`) — did not
  do a corpus-wide topic-clustering pass this cycle given the light
  21-commit delta.
- **No findings:** CONTRADICTION (none found — the one live contradiction
  from two cycles ago remains resolved); STALE-FACT (the `HOW_IT_WORKS.md`
  Tier 2 passages fixed in the 2026-07-09 batch remain accurate: six
  registered adapters, Dell PowerEdge correctly noted as abandoned, no new
  enumerable drift found); DUPLICATION (none new); PROMPT-ROSTER SKEW
  (all 20 prompts consistent with the CLAUDE.md roster, including the two
  newest-touched agents `framework-reviewer` and `content-installer`).
- **Process note (positive):** unlike the 2026-07-09 report, which found
  4 of 5 findings were unaddressed repeats from a prior cycle, this cycle's
  baseline was clean — every 2026-07-09 finding was applied in a single
  follow-up commit (`e0c1de8`, #42) before the reorg PRs landed. The
  mechanism the last report worried about (mechanical curator findings
  not getting applied) does not appear to be recurring.

## Summary for orchestrator

- Counts by class: DRIFT 1 (2 rule files + 1 INDEX line) · INDEX ROT 2
  (api-surface/ 6 missing rows, wire-formats/ 2 missing rows) · SUPERSEDED 1
  (session-handoff.md, untracked) · CITATION-INTEGRITY 1 (DEF-011's
  backlog pointer at risk of going dead).
- Mechanical/judgment split: 3 mechanical / 2 judgment.
- **Highest-value finding:** the CITATION-INTEGRITY one — not because the
  rot exists yet, but because it's a rot-in-waiting with a clear trigger
  (deleting `session-handoff.md`, which is the obviously-correct mechanical
  cleanup) that would silently sever `defects.md`'s DEF-011 cross-reference
  to a concrete, incident-backed fix. Worth fixing in the order given:
  migrate the backlog item to `feedback_queue.md` first, delete second.
- Second-highest: the `api-surface/` INDEX ROT — 6 of 20 files undiscoverable
  from the README, including the root-cause doc for a currently-open
  blocking defect (DEF-012).
- Report path: `knowledge/context/curation/2026-07-16-report.md`.
