# Curation report — 2026-08-19

Corpus scanned: knowledge/rules/ (17 + INDEX.md) · knowledge/lessons/ (25 + INDEX.md) ·
                knowledge/context/ (api-surface/ 23 files + managed_paks.md registry +
                defects.md + feedback_queue.md, targeted) ·
                .claude/agents/ (20) · skills (5)
Since: 2026-08-06 (prior report) — only **2 commits** landed in the window
(`8ad7dd2` VM Snapshot Inventory dashboard + DEF-018 fix, `04f665d` Allow
Insecure SSL hostname-verification fix, issue #82). Neither commit touches
`knowledge/rules/`, `knowledge/lessons/` (except the SSL lesson, which
`04f665d` itself correctly updated — see below), `.claude/agents/`, or skills.
Findings: 7 (2 mechanical / 5 judgment) — all 7 are re-reports of unresolved
2026-08-06 findings. No new rot detected this cycle.

**Process note:** verified all 7 unresolved 2026-08-06 findings by direct
read of current file state (not assumed carried-over). All 7 are still
present, byte-for-byte unchanged where checked line-by-line. This is now
the **second consecutive cycle with zero findings applied** — the
2026-07-27 report flagged the same "zero of 6 findings applied" pattern.
Two findings are now confirmed unresolved across **4+ curation cycles**
(api-surface/ index gap, `vcfops-orchestration` dead skill ref) and one
across at least 3 (compliance `adapterLogger()` SUPERSEDED claim, now
freshly reconfirmed by direct read of `ComplianceAdapter.java` — zero
`adapterLogger()` occurrences, three `componentLogger(...)` call sites).

**One item independently self-corrected this cycle, outside the curator's
own report cycle:** `04f665d`'s commit message states it "corrects stale
claims in SuiteApiStitchClient javadoc and
knowledge/lessons/suite-api-stitch-ssl-tofu-vs-java-http.md that described
a HostnameVerifier limitation this change removes" — confirmed by diff
(17 lines changed in the lesson file) and by framework-reviewer APPROVE
(`knowledge/context/reviews/framework/adapter-framework-insecure-ssl-hostname-2026-08-17.md`).
This is a positive existence proof that the authoring workflow *can* keep
lessons current when the author remembers to — worth noting as a contrast
to the FB-005/design-doc and compliance-shadow items below, which are the
same author-forgot-to-update-the-doc pattern but uncorrected for weeks.

## Mechanical (orchestrator may apply as a small PR)

- **[INDEX ROT]** `knowledge/context/README.md`'s `api-surface/` table
  lists **21** entries; disk has **23** files
  (`ls knowledge/context/api-surface/*.md`). Missing rows, unchanged since
  2026-07-27 (fourth cycle running): `compliance_enablement_markers.md`
  (established 2026-07-23, `6ac04b2`) and
  `resourcelist_column_state_wire_format.md` (established 2026-07-22,
  `9509915`). No new api-surface file landed this cycle. → Add the two
  rows. Given this is now four cycles old on the same table, the standing
  habit-note suggestion from 2026-08-06 (add a line to `knowledge/context/README.md`
  itself: "update this table in the same commit that adds a file under
  `api-surface/`") is worth actually doing rather than re-suggesting a
  fifth time.

- **[DEAD-REF]** `.claude/skills/vcfops-project-conventions/SKILL.md:10`
  (frontmatter `description`) — "For delegation protocol and workflow
  patterns, use vcfops-orchestration instead." No skill named
  `vcfops-orchestration` exists (`ls .claude/skills/` → 5 skills, none by
  this name). Root cause: commit `8e4f13a` (2026-05-09) deleted
  `.claude/skills/vcfops-orchestration/SKILL.md` and folded its content
  into CLAUDE.md's delegation protocol. Unchanged since 2026-08-06 — now
  a 15-week-old stale citation surviving its 8th curation cycle. →
  Reword to "use CLAUDE.md's delegation protocol instead," or drop the
  sentence.

## Judgment (triage — Scott)

- **[SUPERSEDED]** Two locations, same stale claim, unchanged since
  2026-08-06 and reconfirmed by fresh direct read this cycle:
  - `knowledge/context/framework_v2_migration.md:815-818` (§19) —
    "**Compliance** still carries its historical private `adapterLogger()`
    shadow (introduced before `componentLogger` was public). It is the
    target of the pending compliance v2 fixup..."
  - `knowledge/context/tier2_architecture.md:505` (2026-06-10 changelog
    row) — "...Compliance (build 46) has no framework-method shadow to
    remove — its `adapterLogger()` shadow is in `ComplianceAdapter.java`
    and **is the target of the pending compliance v2 fixup**."
  Both contradicted by direct read of
  `content/sdk-adapters/compliance/src/com/vcfcf/adapters/compliance/ComplianceAdapter.java`
  today: zero `adapterLogger()` occurrences, three `componentLogger(...)`
  call sites (lines 135, 148, 151). Shadow was removed in build 49
  (`eba91b2`, "drop shadow logger"); two further builds (50, 51) have
  landed since with no regression, most recently build 51's MOID-scoping
  fix (`stitch-moid-not-unique-across-vcenters.md`). → Update both
  passages; §19 can promote compliance to a second positive exemplar
  alongside Synology, and the tier2_architecture.md changelog row can get
  a one-line superseding footnote (changelog rows are historical record —
  strikethrough/footnote, not rewrite).

- **[SUPERSEDED]** `knowledge/context/feedback_queue.md:92` (FB-005,
  "Synology iSCSI/NFS → VMware Datastore cross-link never persists") —
  unchanged since 2026-08-06. Status line still reads "open — root cause
  revised 2026-06-30... Fix in flight:
  `knowledge/designs/suite-api-stitcher-tls-auth-cleanup-v1.md`." That
  design doc's own status line (line 3, last touched 2026-07-09) still
  says "DECIDED → in implementation." Both are contradicted by
  `knowledge/context/defects.md` DEF-005 (same defect, **Status: closed**,
  closing-evidence build 24, 2026-07-02) and by
  `knowledge/lessons/suite-api-stitch-ssl-tofu-vs-java-http.md`, which
  documents the finalized fix as generalized policy — and which itself
  was actively maintained as recently as `04f665d` (2026-08-17), making
  the FB-005/design-doc staleness more conspicuous by contrast (the
  underlying mechanism is clearly still being tended; the status labels
  describing it are not). → FB-005 should read `closed` (or `resolved`,
  cross-referencing DEF-005); the design doc's status line should read
  `implemented` with a closing pointer to DEF-005 and the lesson. Root
  cause landed 2026-07-02 — this claim has now outlived its fix by
  **seven weeks** and survived at least 3 curation cycles, the closest
  parallel in this corpus to the original `worker.int`/`0f7aad9` pattern
  the curator was built to catch.

- **[CONTRADICTION]** `knowledge/context/authoring/view_dashboard_design_guide.md:318-329`
  ("Dashboard layout conventions" / "Grid system") vs
  `knowledge/context/defects.md` DEF-013 (closed). Unchanged since
  2026-08-06 (reconfirmed by direct read this cycle, lines 310-335): the
  guide still states "`y`: row (0-based, grows downward)" with a worked
  example `coords: {x: 0, y: 0, w: 12, h: 4}` and no caveat, while DEF-013
  proved the wire format is 1-indexed and recommends 1-based authoring
  going forward. This is the read-before-authoring doc, and the
  8ad7dd2 commit in this window shipped a new dashboard
  (`vm-snapshot-inventory-dashboard`) plus a general "Dashboard layout
  conventions" section is exactly what an author would consult — the
  guide's incorrect 0-based example keeps circulating live. Now
  unresolved across **3 full curation cycles** (first flagged
  2026-07-27), still the single highest-value unfixed finding in this
  corpus. → Update lines 320-329 to 1-based coords with a callout citing
  DEF-013's floor-clamp as a backstop, not the spec.

- **[DEAD-REF]** (RULE-015 violation, unchanged) — two durable governance
  docs still cite `memory/` files that are `.gitignore`d and were never
  committed to this repo (`git log --all` confirms none of the three
  paths below were ever tracked):
  - `knowledge/ROADMAP.md:84` — "(see `memory/feedback_powershell_idioms.md`)"
  - `knowledge/ROADMAP.md:138` — "manual-UI MPB tasks tracked under
    `memory/project_mpb_manual_ui_tasks_pending.md`"
  - `knowledge/context/known_limitations.md:100` — "See
    `memory/project_vcf_ops_902_ui_deadends.md`"
  → Per RULE-015 (`knowledge/rules/cited-artifacts-reproducible.md`):
  commit the content if still relevant, inline-summarize, or drop the
  sentence.

- **[PROMPT-ROSTER SKEW]** `.claude/agents/api-cartographer.md` vs
  CLAUDE.md roster row — unchanged. CLAUDE.md grants api-cartographer a
  `reference/docs/` RULE-016 write carve-out ("vendor artifacts may be
  added under `reference/docs/`"); the agent's own frontmatter and Hard
  Rule #2 (line 26: "Write only to `knowledge/context/api-maps/`.") still
  omit the RULE-016 mention, unlike the sibling `api-explorer` prompt,
  which states the carve-out verbatim. Spot-checked 5 other agent prompts
  this cycle (`tooling`, `framework-reviewer`, `curator`,
  `sdk-adapter-reviewer`, `content-installer`) against their roster rows
  — no additional skew found. → Reconcile: either add the carve-out to
  the `api-cartographer` prompt or narrow the roster row.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (17/17, clean,
  bidirectional); `knowledge/lessons/*.md` vs `INDEX.md` (25/25, clean,
  bidirectional); the 20-agent roster cross-checked by name against
  CLAUDE.md's table (exact match, `diff` clean); the `api-surface/` table
  vs disk (21/23, same 2-file gap as prior 3 cycles); the
  `managed_paks.md` registry (6 entries) vs `content/sdk-adapters/` on
  disk (exact match, 6 dirs) and `HOW_IT_WORKS.md`'s "six adapters are
  registered" claim (current, no STALE-FACT); re-verified all 7
  unresolved 2026-08-06 findings by direct read of current file state.
- **Sampled:** PROMPT-ROSTER SKEW — 5 additional agent prompts spot-
  checked beyond the known api-cartographer item, no new skew found; not
  an exhaustive 20-agent pass this cycle given the near-zero commit
  volume in the window. DUPLICATION got a light spot-check only (no new
  investigations/reviews landed this cycle that would create fresh
  overlap candidates beyond the already-known SSL/stitch/MOID lesson
  cluster, which remains non-overlapping on inspection).
- **No findings:** DRIFT (none this cycle; confirmed `content/managementpacks/`
  references across the corpus are legitimate Tier 1 MPB paths, not stale
  Tier 2 references — the directory exists on disk with 5 files, distinct
  from `content/sdk-adapters/`); new INDEX ROT beyond the carried-over
  api-surface gap (rules/lessons indexes both clean); new SUPERSEDED
  (grepped for blocker/TODO/gap language against the 2 commits in this
  window — both commits' own doc updates were self-consistent, no new
  stale claim introduced); new STALE-FACT (adapter count, corpus-size
  language all current).

## Summary for orchestrator

- Counts by class: INDEX ROT 1 (carried over, 4th cycle) · DEAD-REF 2
  (skill citation carried over 8th cycle; RULE-015 violation, 3 citations
  across 2 files, carried over) · SUPERSEDED 2 (compliance-shadow claim,
  2 surfaces, carried over 3rd+ cycle; FB-005/design-doc status, carried
  over, now 7 weeks stale) · CONTRADICTION 1 (dashboard 0-based coords,
  carried over 3rd cycle) · PROMPT-ROSTER SKEW 1 (api-cartographer,
  carried over).
- Mechanical/judgment split: 2 mechanical / 5 judgment.
- **Highest-value finding:** unchanged — the CONTRADICTION in
  `knowledge/context/authoring/view_dashboard_design_guide.md`'s 0-based
  coords guidance, now unresolved across 3 full curation cycles. It is
  read *before* authoring, and this window shipped a new dashboard that
  would have consulted exactly this section.
- **Second-highest:** the FB-005 SUPERSEDED finding — a resolved-blocker
  claim (DEF-005 closed 2026-07-02) still circulating as "in flight,"
  now 7 weeks stale. Sharpened this cycle by contrast: the underlying
  fix's own lesson file (`suite-api-stitch-ssl-tofu-vs-java-http.md`) was
  actively and correctly updated on 2026-08-17 by an unrelated commit,
  proving the corpus *can* stay current when authors remember — the
  status labels are the part nobody circled back to.
- **Process flag for the orchestrator:** this is the **second consecutive
  cycle with zero of the prior cycle's findings applied** (2026-08-06 had
  the same flag for the 2026-07-27 findings). Two items are now 4+ and 8
  cycles old respectively. If the "orchestrator may apply as a small PR"
  half of this agent's contract is not being exercised, the mechanical
  findings queue will keep growing without bound — worth a direct check-in
  with Scott on whether a small batch PR for the 2 mechanical items
  (api-surface index rows + the dead skill citation) should just be
  scheduled now, since both are single-line, low-risk edits with no
  judgment call attached.
- Report path: `knowledge/context/curation/2026-08-19-report.md`.
