# Curation report — 2026-08-19

Corpus scanned: knowledge/rules/ (17 + INDEX.md) · knowledge/lessons/ (25 + INDEX.md) ·
                knowledge/context/ (306 tracked `.md` files) ·
                .claude/agents/ (20) · skills (5)
Since: 2026-08-06 (prior report) — **2 commits** landed in the window
(`8ad7dd2`, 2026-08-07 — VM Snapshot Inventory dashboard release + DEF-016/017/018
work; `04f665d`, 2026-08-17 — adapter-framework insecure-SSL hostname-mismatch
fix). Neither touches `knowledge/rules/`, `.claude/agents/`, or skills; `8ad7dd2`
touches `knowledge/lessons/INDEX.md` + `pak-content-localization-bundles.md`
(self-consistent narrowing, no rot introduced) and `knowledge/context/defects.md`
(introduces one new internal contradiction, see below); `04f665d` touches
`knowledge/lessons/suite-api-stitch-ssl-tofu-vs-java-http.md` (self-correcting —
verified clean).
Findings: 9  (2 mechanical / 7 judgment)

**Process note (not a hunt-list class, but load-bearing):** re-verified all 7
findings from the 2026-08-06 report by direct read of current file state
(not assumed carried-over). **Zero of the 7 were applied.** This is the
second consecutive cycle with a full carry-over of zero fixes (2026-07-27 →
2026-08-06 was the first). The CONTRADICTION on dashboard coords is now
unresolved across **three full curation cycles** (first flagged 2026-07-27,
confirmed again 2026-08-06, confirmed again today) and the `vcfops-orchestration`
dead skill citation has now survived **8 curation cycles** (first flagged well
before 2026-07-27). Re-reported below rather than silently dropped. One
genuinely new finding this cycle: an internal `Status: closed` vs.
in-body `"the entry stays open"` self-contradiction added to `defects.md`
DEF-018 in the same commit (`8ad7dd2`) — the closing fields were appended
after the Summary prose was written, same day, and the prose sentence was
never revised to match. Also new: a ROADMAP.md STALE-FACT — its "In
Progress"/"Future" sections describe Tier 2 SDK adapters and specific MPs
(Synology, UniFi, GitLab) in states that no longer match reality and haven't
been substantively edited since at least 2026-07-09 (`cfd5390`, a reorg move
only).

## Mechanical (orchestrator may apply as a small PR)

- **[INDEX ROT]** `knowledge/context/README.md`'s `api-surface/` table
  (`### \`api-surface/\`` section, lines 103-126) lists **21** entries; disk
  has **23** files (`ls knowledge/context/api-surface/*.md`). Still missing,
  unchanged since 2026-07-27 (now a **4th** consecutive cycle):
  `compliance_enablement_markers.md` (established 2026-07-23, `6ac04b2`) and
  `resourcelist_column_state_wire_format.md` (established 2026-07-22,
  `9509915`). No new api-surface file landed in this 2-commit window, so
  the miss is purely carried-over neglect. → Add the two rows. Given this
  is now 4-cycles-old debt on the same table, the standing-habit-note
  suggestion from 2026-08-06 (update the table in the same commit that adds
  a file under `api-surface/`) is worth actually adding this time, not just
  re-suggesting.

- **[DEAD-REF]** `.claude/skills/vcfops-project-conventions/SKILL.md:10`
  (frontmatter `description`) — "For delegation protocol and workflow
  patterns, use vcfops-orchestration instead." No skill named
  `vcfops-orchestration` exists (`ls .claude/skills/` → 5 skills, none by
  this name; corpus-wide grep for the string returns only this one hit).
  Root cause: commit `8e4f13a` (2026-05-09) deleted
  `.claude/skills/vcfops-orchestration/SKILL.md` and folded its content into
  CLAUDE.md's delegation protocol — the deliberate, stated design. Byte-
  for-byte unchanged since 2026-08-06; now an **8-cycle-old** stale citation.
  → Reword to "use CLAUDE.md's delegation protocol instead," or drop the
  sentence.

## Judgment (triage — Scott)

- **[SUPERSEDED]** Two locations, same stale claim, unchanged since
  2026-08-06 (3rd cycle):
  - `knowledge/context/framework_v2_migration.md:815-818` (§19) — "Compliance
    still carries its historical private `adapterLogger()` shadow... target
    of the pending compliance v2 fixup."
  - `knowledge/context/tier2_architecture.md:505` (2026-06-10 changelog row) —
    "Compliance (build 46)... its `adapterLogger()` shadow is in
    `ComplianceAdapter.java` and **is the target of the pending compliance
    v2 fixup**."
  Both contradicted by `content/sdk-adapters/compliance`'s own git history
  (build 49, commit `eba91b2`, "drop shadow logger") and by direct read of
  `ComplianceAdapter.java` today (zero `adapterLogger()` shadow; every
  acquisition goes through `componentLogger(...)`). The adapter's own repo
  has moved on further since the last report — `content/sdk-adapters/compliance`
  is now at build 54+ (`19739a0`, "preserve hand-build 52-54 CHANGELOG
  history"), five builds past the build-49 fix with no regression. →
  Update both passages; §19 can promote compliance to a second positive
  exemplar, and the `tier2_architecture.md` changelog row can get a
  one-line superseding note (changelog rows are historical record, so the
  fix there is a footnote/strikethrough, not a rewrite).

- **[SUPERSEDED]** `knowledge/context/feedback_queue.md:92` (FB-005,
  "Synology iSCSI/NFS → VMware Datastore cross-link never persists") —
  unchanged since 2026-08-06. Status line still reads "open — root cause
  revised 2026-06-30... Active prod blocker is the framework loopback Suite
  API transport... Fix in flight:
  `knowledge/designs/suite-api-stitcher-tls-auth-cleanup-v1.md`." That
  design doc's own status line (last touched 2026-07-09) still says
  "DECIDED → in implementation." Both are contradicted by
  `knowledge/context/defects.md` DEF-005 (same defect, **Status: closed**,
  closing-evidence build 24, 2026-07-02) and by
  `knowledge/lessons/suite-api-stitch-ssl-tofu-vs-java-http.md`, which
  documents the finalized fix as generalized policy — and which was itself
  *updated this cycle* (`04f665d`, 2026-08-17) to record that the
  hostname-verifier gap this design doc references is now **also** closed
  (issue #82). The design doc is now stale on two independent axes (the
  TOFU transport fix from 2026-07-02 and the hostname-verifier fix from
  2026-08-17), both landed, neither reflected. → FB-005 should read
  `closed`/`resolved` cross-referencing DEF-005; the design doc's status
  line should read `implemented` with pointers to both closing commits.

- **[SUPERSEDED / self-contradiction, new this cycle]**
  `knowledge/context/defects.md` DEF-018 — the entry's own `Status` field
  (line 991: `**Status:** closed`) and `Closing-evidence` (line 1065:
  "2026-08-06 `content-packager` rebuild: all 12 buildable manifests...
  rebuilt... Framework-reviewer verdict APPROVE") directly contradict a
  sentence in the same entry's Summary (line 1054): "**The framework-code
  fix is done; the entry stays open because the fix is not yet in an
  operator's hands.**" Confirmed via `git log -p` on `knowledge/context/defects.md`
  that both the Summary prose and the later Status/Closing-evidence fields
  were added in the **same commit** (`8ad7dd2`) — the Summary was evidently
  drafted mid-session before the rebuild closed the entry, and the sentence
  was never revised to match the final Status. The `Close condition`
  paragraph immediately above the contradicting sentence describes exactly
  the rebuild that the `Closing-evidence` field then reports as done — the
  entry closed itself out from under its own prose in one sitting. → Strike
  or rewrite the "the entry stays open..." sentence to past tense
  ("...was open until the 2026-08-06 rebuild; see Closing-evidence").
  Low-risk rewrite, but leaving it for Scott since it's prose surgery
  inside a registry entry, not a mechanical path/index fix.

- **[STALE-FACT, new this cycle]** `knowledge/ROADMAP.md` — the "In
  Progress" (line 113) and "Future" (line 192) sections describe project
  state that reality has moved well past, and the file's content has not
  been substantively edited since `cfd5390` (2026-07-09, a directory-reorg
  move, not a content update):
  - Line 194, "### Operations SDK management packs" (under `## Future`):
    "Candidate as the second-tier MP path once MPB coverage stabilizes and
    we hit a target API the MPB DSL can't cleanly express." Tier 2 (Ops
    SDK) is not a future candidate — it is **in production today**: six
    adapters registered (`knowledge/context/managed_paks.md`,
    `content/sdk-adapters/` on disk — compliance, synology, unifi,
    vcommunity, vcommunity-os, vcommunity-vsphere), `knowledge/HOW_IT_WORKS.md:264`
    ("Tier 2 is in production: six adapters are registered"), a full
    CLAUDE.md workflow section ("Management pack (Tier 2 Java SDK)"), two
    dedicated agents (`sdk-adapter-author`, `sdk-adapter-reviewer`), and a
    CI release pipeline (RULE-012/014, `sdk-adapter-author.md`).
  - Line 122-129, "### Management pack authoring (MPB-compiled `.pak`)"
    (under `## In Progress`) still describes Synology as "first MP, used to
    shake out the framework" and UniFi as "second MP, framework-
    generalization test... API mapped against the devel controller" —
    language for an early exploratory MPB (Tier 1) pass. Both are in fact
    mature, shipping Tier 2 SDK adapters with dozens of builds each
    (`knowledge/lessons/synology-dsm-client-side-joins.md`: "First Tier 2
    build: 9 paks, 8 structure fixes, 20 commits"; `unifi-metric-key-parity.md`).
  - Line 130, same section: "**GitLab** — untracked third MP under
    `managementpacks/` exploring CI/CD observability." No GitLab management
    pack exists anywhere in the repo today — `content/managementpacks/`
    contains `cloudflare.yaml`, `dell_poweredge.yaml`, `unifi_network.yaml`,
    `unifi_network_integration.yaml`, `vsphere_storage_paths.yaml`; the only
    GitLab references in the whole tree are HoL lab-file fixtures under
    `reference/references/`. This candidate appears to have been dropped
    with no trace left in the roadmap.
  → All three are enumerable lifecycle-status claims (not prose judgment):
  a "candidate/future" label on a shipping capability, a "first/second MP
  learning target" label on production adapters, and a named project that
  no longer exists anywhere in the tree. Reword or move to "## Done";
  consider whether ROADMAP.md is still a living doc or should carry the
  same "timestamped record, not a living doc" caveat `reviews/` gets, if
  nobody intends to keep it current.

- **[CONTRADICTION]** `knowledge/context/authoring/view_dashboard_design_guide.md:318-329`
  ("Dashboard layout conventions" / "Grid system") vs `knowledge/context/defects.md`
  DEF-013 (closed). Unchanged since 2026-07-27 — now **3 full cycles**
  unresolved. The guide still states "`y`: row (0-based, grows downward)"
  with a worked example `coords: {x: 0, y: 0, w: 12, h: 4}` and no caveat,
  while DEF-013 proved the wire format is 1-indexed ("Every widget
  coordinate in the known-good corpus... uses `x >= 1` and `y >= 1`; zero
  use 0 for either axis") and recommends 1-based authoring going forward.
  This is the read-before-authoring doc; still the highest-value unresolved
  finding across three cycles running. → Update lines 320-329 to 1-based
  coords with a callout citing DEF-013's floor-clamp as a backstop, not the
  spec.

- **[DEAD-REF]** (RULE-015 violation, unchanged) — two durable governance
  docs still cite `memory/` files that are `.gitignore`d and were never
  committed to this repo (`git log --all` confirms):
  - `knowledge/ROADMAP.md:84-85` — "(see `memory/feedback_powershell_idioms.md`)"
  - `knowledge/ROADMAP.md:138` — "manual-UI MPB tasks tracked under
    `memory/project_mpb_manual_ui_tasks_pending.md`"
  - `knowledge/context/known_limitations.md:100` — "See
    `memory/project_vcf_ops_902_ui_deadends.md`"
  → Per RULE-015 (`knowledge/rules/cited-artifacts-reproducible.md`): commit
  the content if still relevant, inline-summarize, or drop the sentence.

- **[PROMPT-ROSTER SKEW]** `.claude/agents/api-cartographer.md` vs
  CLAUDE.md roster row — unchanged. CLAUDE.md grants api-cartographer a
  `reference/docs/` RULE-016 write carve-out ("vendor artifacts may be
  added under `reference/docs/`"); the agent's own frontmatter and Hard
  Rule #2 (line 26: "Write only to `knowledge/context/api-maps/`") still
  make no mention of RULE-016, in contrast to the sibling `api-explorer`,
  whose prompt states the carve-out verbatim. → Reconcile: either add the
  carve-out to the prompt or narrow the roster row.

## Coverage notes

- **Fully scanned:** `knowledge/rules/*.md` vs `INDEX.md` (17/17, clean,
  bidirectional); `knowledge/lessons/*.md` vs `INDEX.md` (25/25, clean,
  bidirectional); the 20-agent roster frontmatter cross-checked against
  CLAUDE.md's table (20/20, one skew carried over); the 5 skill
  frontmatters (1 dead cross-reference, carried over); the full
  `knowledge/context/README.md` index — every `### \`dir/\`` section with a
  file table checked bidirectionally against disk: `mpb/` (18/18 clean),
  `wire-formats/` (9/9 clean), `authoring/` (18/18 clean), `api-surface/`
  (21 indexed / 23 disk — rot, reported), plus directory-level (no-table)
  sections (`api-maps/`, `cleanroom-spec/`, `specimens/`,
  `adapter_describe_cache/`, `exports/`, `investigations/`, `reviews/`,
  `cleanroom-requests/`, `attic/`) spot-verified against disk with no
  drift; `cleanroom-spec/`'s "start at spec/17... spec/15" pointers
  confirmed both files exist; `managed_paks.md`'s 6-entry registry vs
  `content/sdk-adapters/` on disk (exact match) and
  `HOW_IT_WORKS.md`'s "six adapters are registered" claim (current); the
  2-commit delta's full diff read in both commits (`8ad7dd2`, `04f665d`)
  line-by-line for every touched governance file; DEF-016/017/018 read in
  full and cross-checked against source code (`src/vcfops_packaging/cli.py`
  confirms DEF-017 is still genuinely open — the unguarded
  `art.zip_path.name` line is unchanged); re-verified all 7 findings from
  the 2026-08-06 report by direct read of current state, not assumed
  carried-over.
- **Sampled, not exhaustive:** DUPLICATION got a spot-check only (the
  SSL/stitch/MOID lesson cluster re-confirmed non-overlapping; no new
  lessons landed this cycle to cluster against). STALE-FACT was scoped to
  enumerable claims; beyond `ROADMAP.md` (new finding) and the two
  registries already checked, no full sweep of every version/count string
  in the 306-file `knowledge/context/` tree was attempted — this remains a
  standing gap in coverage the design doc anticipated ("time-boxed or
  sampled a large class, say what you did not reach").
- **No findings:** DRIFT (none this cycle; no path moves/renames landed in
  the 2-commit window — `content/managementpacks/` still exists and is
  correctly Tier-1-live, not a stale reference); new INDEX ROT beyond the
  carried-over `api-surface/` gap (rules/lessons/mpb/wire-formats/authoring
  indexes all clean); new PROMPT-ROSTER SKEW beyond the carried-over
  api-cartographer item (no new/edited agent prompts this cycle).

## Summary for orchestrator

- Counts by class: INDEX ROT 1 (carried over, 4th cycle) · DEAD-REF 2
  (1 skill citation carried over 8th cycle, 1 RULE-015 violation — 3
  citations across 2 files — carried over) · SUPERSEDED 3 (compliance-
  shadow claim carried over 3rd cycle across 2 surfaces; FB-005/design-doc
  status carried over; DEF-018 self-contradiction, **new**) ·
  CONTRADICTION 1 (dashboard 0-based coords, carried over, now 3rd cycle)
  · STALE-FACT 1 (ROADMAP.md Tier-2/MP lifecycle language, **new**) ·
  PROMPT-ROSTER SKEW 1 (api-cartographer, carried over).
- Mechanical/judgment split: 2 mechanical / 7 judgment.
- **Highest-value finding:** still the CONTRADICTION —
  `knowledge/context/authoring/view_dashboard_design_guide.md`'s 0-based
  coords guidance, now unresolved across **three full curation cycles**
  (first flagged 2026-07-27). It is read *before* authoring, so it keeps
  steering the next dashboard author into the exact bug DEF-013 already
  spent a Playwright pass and a renderer patch fixing. Given zero of the
  last two cycles' findings were applied at all, this is the single item
  most worth hand-fixing regardless of process questions below.
- **New and worth flagging on its own:** the ROADMAP.md STALE-FACT. It's
  not a subtle drift — "Operations SDK management packs" is filed under
  `## Future` as a hypothetical while it has been shipping in production
  for weeks with its own CI pipeline, two dedicated review agents, and a
  release gate rule. A new contributor reading ROADMAP.md top-to-bottom
  would materially misunderstand where the project is.
- **Process flag for the orchestrator (repeated, now stronger):** this is
  the **second consecutive cycle with zero of the prior cycle's findings
  applied** (2026-07-27 → 2026-08-06 was the first). Combined, that means
  some items (the `vcfops-orchestration` dead skill ref, the api-surface
  index gap, the dashboard-coords contradiction) have now survived 3-4
  reported cycles each without action. The "orchestrator may apply as a
  small PR" half of this agent's contract does not appear to be
  exercised in practice — worth a direct check-in with Scott on whether
  that follow-up step is happening, or whether the mechanical findings
  should just be batched into this report's own follow-up PR next cycle
  regardless of who triggers it.
- Report path: `knowledge/context/curation/2026-08-19-report.md`.
