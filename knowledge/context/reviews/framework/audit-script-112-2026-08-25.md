# Framework review: path_reference_audit.sh subprocess hoist (#112)

- **Branch/commit:** fix/112-audit-speed, bf07a2e vs origin/main
- **Surface:** scripts/path_reference_audit.sh (+ new lesson
  knowledge/lessons/no-subprocess-in-per-item-shell-loops.md, INDEX row)
- **Reviewer:** framework-reviewer, 2026-08-25
- **Verdict:** APPROVE (0 BLOCKING, 2 WARNING, 1 NIT)
- **Ship bar:** output byte-identical before/after, or no ship.

## Independent verification (author artifacts not trusted; all re-run)

Old script extracted from `git show origin/main:scripts/path_reference_audit.sh`
and run side by side with the new one on the same tree; stdout, stderr,
and exit code diffed per state. Tree restored to clean after every probe
(`git status --porcelain` empty).

| State | Result |
|---|---|
| Clean tree | stdout identical (sole diff was `$0` basename of my extracted copy; re-run with matching filename: byte-identical), stderr byte-identical incl. both RULE-015 tvs WARNINGs, exit 0=0. Old 3m08s, new 3.0s cold. |
| Probe A: 7 injected dead refs (two on one line in CLAUDE.md incl. trailing-punct strip; dot-metachar path `knowledge/dead.file.md`; dead 2-seg dir `knowledge/lessonz`; cross-file dead ref in a nested rules file; two dead `knowledge/` refs on one line in tests/conftest.py exercising the restructured second pass; fourth-pass bare-dir token `` `investigations/` `` in an agent file with suggestion output; fourth-pass `/seg/`-substring suppress branch via `` `skills/` `` in a skill file). Valid-side branches on the same run: tracked-DIRECTORY citation `src/vcfops_dashboards` (TRACKED_DIR_PREFIX vs old `--error-unmatch` dir pathspec), `.md` suffix branch (`knowledge/HOW_IT_WORKS`), `.py` suffix branch (`src/vcfops_common/doctor`). | stdout, stderr, exit (2=2) byte-identical; report ordering stable. |
| Probe C: non-ASCII path (`knowledge/context/tmp-ü-probe.md`) added to the git index + one injected dead ref | byte-identical, exit 2=2. Confirms `-z`/`read -d ''` table build is inert. Note: no non-ASCII candidate can ever be extracted (candidate char classes are ASCII-only), so lookups are safe by construction as well. |
| Regex-metachar / space paths | Spaces and glob/regex metachars other than `.` `-` `_` are unreachable by construction: the extraction classes `[A-Za-z0-9_.\/-]` / `[A-Za-z0-9_-]` cannot emit them, so neither version's tracked-lookup or suggestion path ever sees one. Dot covered live in Probe A. TOPLEVEL_RE escaping: new pure-bash loop escapes the identical char set as the old sed (backslash first), verified by inspection and by identical rule-1 behavior in all runs. |
| Probe B: adversarial seams (below) | two divergences found, both outside any reachable current-tree state. |

Other checks: shellcheck 0.11.0 old-vs-new diff shows the same two
SC2016 *info* findings in both (relocated lines only), no new findings.
Seven-package validate chain re-run: green (exit 0). Fourth-pass
suggestion equivalence proven by inspection (`${tf%%"/${seg}/"*}` = old
non-greedy `^.*?/${seg}/` first-occurrence cut, same ls-files order) and
by identical Probe A suggestion output.

## Findings

### WARNING W1: `.gitignore` prev-line semantics changed (rule #4 marker source)
`scripts/path_reference_audit.sh` main loop. Old code `continue`d past
non-comment `.gitignore` lines BEFORE the `prev_line="${line}"` update, so
for a `.gitignore` comment line, `prev_line` was the last *comment* line.
New code uses the literal previous line (`file_lines[lineno-2]`), which can
be an ignore *pattern*. Proven divergence (Probe B): pattern line ending in
`example` followed by a comment citing a dead path -> old reports the dead
ref, new suppresses it via the example-marker heuristic. Unreachable on the
current `.gitignore` (clean-tree run is byte-identical), and the new
behavior arguably matches rule #4's documented "line just above it" intent,
but it is an unclaimed behavior change that can *swallow* a dead reference.
Smallest fix if strict identity is wanted: in the `.gitignore` branch,
derive `prev_line` from the last preceding comment line instead of
`lineno-2`. Acceptable alternative: document the delta in the script header.

### WARNING W2: final line without trailing newline is now audited
Old `while IFS= read -r line` silently dropped a file's last line when it
lacked a trailing newline (read returns nonzero); `mapfile` keeps it.
Proven divergence (Probe B): dead ref on an unterminated final line of an
agent file -> old silent, new reports it (exit 2). Every current corpus
file is newline-terminated, so clean-tree identity holds. This is a latent
bug *fix* (the audit now sees a line it used to miss), but it is unclaimed;
worth one line in the script header or PR description so a future
"the audit newly flags X" is not mistaken for a regression.

### NIT N1: lesson overstates "byte-identical"
`knowledge/lessons/no-subprocess-in-per-item-shell-loops.md` and its INDEX
row state byte-identical output unqualified. True for every state the
author tested and every realistic state I tested, but W1/W2 are real
divergences on synthetic inputs. A one-clause caveat ("on the current
corpus and injected-dead states") would make the lesson exact. Lesson file
otherwise conforms to knowledge/lessons/ conventions (kebab name, Date/Cost
header, INDEX row added, newest-first).

## If shipped as-is
CI gate drops from ~3m (6m on failure path) to ~3s with identical verdicts
on every state the repo can currently reach; the only observable deltas
require a `.gitignore` pattern line ending in an example marker or an
unterminated final corpus line, and in the latter case the new script
catches a dead reference the old one missed.

---

## Re-review addendum: amended commit dc3ca33 (2026-08-25)

All three findings were folded in; re-verified independently:

- **W1 RESOLVED (verified).** `.gitignore` prev_line now scans backward
  for the last preceding comment line (literal previous line everywhere
  else), restoring the old loop's semantics exactly, including the
  no-preceding-comment -> empty case. Divergence-1 probe re-run against
  dc3ca33 (pattern line `deadpattern-example` + comment citing
  `knowledge/context/gitignore-dead.md`): old and new both REPORT the
  dead ref, stdout/stderr byte-identical, exit 2=2.
- **W2 RESOLVED (documented).** Script header now carries the "Known
  behavior delta" note for the unterminated-final-line case; accurate as
  written ("everything else is byte-identical" holds across all my
  probes at dc3ca33).
- **N1 RESOLVED.** Lesson and INDEX row now carry the qualifying clause
  naming the sole deliberate delta; the added "Proving the rewrite"
  sentence about `continue`-carried state correctly describes the trap
  W1 exposed.
- **Nondeterminism sentence:** the lesson's claim (a transiently failed
  fork under `2>/dev/null` masking turned a suppression check into a
  spurious dead-reference finding twice during #112 verification) is
  mechanism-accurate from the old code: `grep ... 2>/dev/null || true`
  and suppressed `git ls-files` calls treat a failed fork as
  no-match/untracked, flipping a valid citation to dead. I did not
  observe it across my seven old-script runs (all outputs were exactly
  the expected findings), so I cannot independently reproduce the 2-of-5
  rate; recorded as plausible-and-unrefuted, and it strengthens (not
  weakens) the case for the rewrite.
- Clean-tree re-check at dc3ca33: byte-identical stdout/stderr, exit
  0=0. Shellcheck: same two SC2016 infos, line numbers shifted only.
  Tree restored clean after all probes.

**Final verdict: APPROVE (0 BLOCKING, 0 WARNING, 0 NIT outstanding).**
