# Framework review: install.ps1 #124 warn-and-continue (fix/124-uninstall-warn-and-continue vs 81ec642)

Reviewer: framework-reviewer. Date: 2026-08-25.
Round 1: b98f4da reviewed, CHANGES REQUESTED (1 BLOCKING).
Round 2: delta 28e0bc0 reviewed, verdict APPROVE.

## Change under review

Three inner residual-else sites in the shipped uninstall path of
`src/vcfops_packaging/templates/install.ps1` now emit a Write-Warn
advisory naming the observed shape and continue (issue #124):

1. Get-AllViews: view type whose value is not a PSCustomObject.
2. Get-AllViews: subject whose value is not an array.
3. Get-AllReports flatten fallback: object with no recognised key and
   no array-valued property (`$sawArrayProp` presence guard).

Plus harness coverage in `tests/fixtures/install_ps1_advisory_harness.ps1`,
static pins in `tests/test_install_ps1_tls_and_advisories.py`, and
(round 2, commit 28e0bc0) the `CURRENT_TEMPLATE_VERSION` bump to
"2026-08-24-2" in `src/vcfops_packaging/template_version.py`.

## Verdict: APPROVE (0 BLOCKING, 0 WARNING, 2 NIT/notes)

## Checks re-run (independently, both rounds)

- Seven-package validate chain: exit 0 (round 1 and round 2; the
  label-lint WARNs in managementpacks validate are pre-existing corpus
  warnings, unrelated to this diff).
- Full pytest default markers: 1002 passed, 4 skipped (both rounds).
- Targeted: `tests/test_template_version_staleness.py` +
  `tests/test_install_ps1_tls_and_advisories.py`: 72 passed (matches
  tooling's claim).
- pwsh harness vs the template: ALL ASSERTIONS PASSED, exit 0.
- Mutation probes (my own, on scratchpad copies):
  - A: `if (-not $sawArrayProp)` -> `if ($items.Count -eq 0)`:
    harness fails the empty-array-under-unknown-key silence assertion.
  - B: site-2 Write-Warn -> Write-Fail: harness fails "does not throw
    and does not Write-Fail". Abort hazard is pinned.
  - C: null guard removed before `.GetType()` at site 2: harness fails
    the StrictMode null assertion.
- ASCII: `grep -P '[^\x00-\x7F]'` over both touched script files: clean.

## Round 2 verification of the former BLOCKING

- 28e0bc0 is a one-line diff: `CURRENT_TEMPLATE_VERSION = "2026-08-24-2"`.
- Import check: `from vcfops_packaging import CURRENT_TEMPLATE_VERSION`
  prints 2026-08-24-2; builder.py:729, discrete_builder.py:850, and
  cli.py check-staleness (530-541) all read the constant, never a literal.
- `grep -rn "2026-08-24-1" src/ tests/ bundles/`: zero hits; no stale
  reference to the old version string anywhere.
- No test pins the literal (confirmed by grep and by 72/72 passing
  post-bump), matching tooling's claim.

With the bump, every bundle built at 2026-08-24-1 or earlier now
compares stale in check-staleness, so distributed copies carrying the
silent-drop uninstall are told to rebuild. The reports-green-while-
broken hazard is closed.

## Directed hunts (round 1, unchanged by the delta)

- (a) New abort path: none. Only Write-Warn (Write-Host wrapper, no
  exit) added; outer #116/#123 Write-Fail refusals untouched;
  test_advisories_warn_and_never_exit pins per-branch that no
  Write-Fail shares an advisory statement.
- (b) Still-silent shape: only the pre-existing flatten semantics
  (see NIT). All three issue-named sites are now visible.
- (c) StrictMode adversarial shapes: null guarded at both GetType
  sites; string/int hit GetType safely; empty nested objects take the
  loop bodies zero times. `PSObject.Properties[$key]` probe pattern
  per guide_powershell.md.
- (d) PS 5.1: no `?.`, no ternary, no `??`; if-expression assignment,
  `-join`, multi-line parenthesized concat are 5.1-legal per
  knowledge/context/authoring/guide_powershell.md. ASCII only.
- (e) False alarm on clean uninstall: the observed honest-empty report
  shape (`records:[]`, per
  origin/docs/recon-empty-collection-shapes:knowledge/context/api-surface/uninstall_empty_collection_shapes.md)
  returns via the recognised-key loop before the fallback; empty
  grouped object `{}` produces zero advisories. Both are harness
  negative controls and both pass.

## NIT / notes

- [src/vcfops_packaging/templates/install.ps1:~2285] Mixed envelope in
  the Get-AllReports fallback: a recognised key with a NON-array value
  (e.g. `{"records": null, "other": [x]}`) is silently ignored when a
  sibling property is array-valued. Pre-existing flatten semantics,
  not introduced here; candidate for the issue's "later upgrade".
- Post-merge obligation (orchestrator, CLAUDE.md "After tooling
  changes"): delegate a full `content-packager` rebuild of every
  manifest in `bundles/`. The round-1 WARNING (change did not flag the
  rebuild) is resolved by mechanism: the version bump is itself the
  staleness signal, and 28e0bc0's message states the shipped-bundle
  consequence explicitly. The rebuild still has to happen.

## If shipped as-is

Uninstalls become honest at all three sites; previously distributed
bundles now compare stale and prompt their operators to rebuild.
