# attic/legacy-root-content — pre-`content/`-era YAML

Six YAML files that lived in the repo-root `dashboards/` and `views/`
directories before authored content moved under `content/` (mssql +
oracle query-performance dashboards and their views). Retained for
historical context; **not** loaded, validated, or packaged by any
`vcfops_*` CLI. Do not add new content here — authored content goes in
`content/<type>/`.

Archived here by reorg v2 phase 0 (`designs/reorg-v2-landing-page.md`)
when the two legacy root directories were removed from the landing page.
