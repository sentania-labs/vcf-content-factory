# views/ — legacy reference files

This directory is NOT the content source for VCF Ops views.

Content authors: use `content/views/` for all view YAML files that
the factory authors, validates, and packages.

This directory contains legacy reference files retained for historical
context. Do not add new content here.
